node_modules
.next
.env
.env.local
.env.local
.env.production
.env*.local
*.local

# Artefacts Playwright
test-results/
playwright-report/
.data-cache/
restore_output.log
supabase/.temp/
scripts/data/out/corpus-v3-chunks/
scripts/data/out/corpus-v3-source-name-chunks/

# Harnais de diagnostic jetables des relecteurs. Ils écrivent leurs relevés
# sous /tmp et n'ont donc aucun sens en intégration continue : l'un d'eux y a
# échoué sur un chemin absent de la machine de CI. Ce qu'ils établissent
# durablement se verrouille dans un vrai test, jamais dans le harnais.
tests/**/__*.test.js
tests/**/_*.test.js
tests/**/zz-*.test.js
# Dossiers de mesure jetables : un agent en écrit un, le supprime en fin de
# course, et la suite qui tourne pendant ce temps échoue sur « Cannot find
# module ». Ils ne doivent jamais entrer au dépôt.
tests/tmp-*/
