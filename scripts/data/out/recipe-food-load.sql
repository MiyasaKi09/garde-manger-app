-- Recipe V3 food forms — generated by build-recipe-food-sql.mjs.
-- Candidate-only: missing storage truth remains an explicit review task.
-- configuration_hash = 6cad0dc95a0f297f615e18b55b3e41a3

INSERT INTO ops.source_datasets
  (code, name, publisher, source_url, license_code, allowed_uses,
   update_strategy, current_version, last_checked_at)
VALUES
  ('myko_recipe_food_v3', 'Formes alimentaires résolues pour le corpus V3',
   'Myko + Anses Ciqual', 'https://ciqual.anses.fr/', 'etalab-2.0',
   '{"store_raw":true,"redistribute":true,"modify":true,"attribution_required":true}'::jsonb,
   'manual_review', 'v3-2026-07', now())
ON CONFLICT (code) DO UPDATE SET current_version = EXCLUDED.current_version, last_checked_at = now();

INSERT INTO ops.import_runs
  (source_dataset_id, source_version, code_version, configuration_hash,
   status, started_at, completed_at, candidate_count)
SELECT id, 'v3-2026-07', 'recipe-food-loader-1.0', '6cad0dc95a0f297f615e18b55b3e41a3',
       'completed', now(), now(), 515
FROM ops.source_datasets
WHERE code = 'myko_recipe_food_v3'
  AND NOT EXISTS (SELECT 1 FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3');

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'agneau hache cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Agneau haché cru', 'agneau hache cru', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Agneau haché cru', 'agneau hache cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '21502:agneau hache cru', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 128, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.48, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.13, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Agneau haché cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '21502', to_jsonb('Agneau, gigot, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ail cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ail cru', 'ail cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ail cru', 'ail cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11000:ail cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 111, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 5.31, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 18.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ail cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11000', to_jsonb('Ail, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ail cru entier' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ail cru entier', 'ail cru entier', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ail cru entier', 'ail cru entier',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11000:ail cru entier', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 111, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 5.31, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 18.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ail cru entier', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11000', to_jsonb('Ail, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ail cru tranche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ail cru tranché', 'ail cru tranche', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ail cru tranché', 'ail cru tranche',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11307:ail cru tranche', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 130, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 22, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ail cru tranché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11307', to_jsonb('Ail, sauté/poêlé, sans matière grasse'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'aioli' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Aïoli', 'aioli', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Aïoli', 'aioli',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11168:aioli', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 392.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.13, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 41, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.42, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Aïoli', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11168', to_jsonb('Sauce aïoli, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ajwain graines' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ajwain graines', 'ajwain graines', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ajwain graines', 'ajwain graines',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11064:ajwain graines', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 334, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 14.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 38, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ajwain graines', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11064', to_jsonb('Carvi, graine'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'algue gim nori' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Algue gim nori', 'algue gim nori', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Algue gim nori', 'algue gim nori',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20987:algue gim nori', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 255, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 31.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 10.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.63, 'g', 'estimated'),
      (v_profile, 'fiber_g', 36.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Algue gim nori', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20987', to_jsonb('Nori (Porphyra sp.), séchée ou déshydratée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'amande' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Amande', 'amande', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Amande', 'amande',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15000:amande', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 574.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 9.51, 'g', 'estimated'),
      (v_profile, 'fat_g', 51.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 12.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Amande', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15000', to_jsonb('Amande (avec peau)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'amande effilee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Amande effilée', 'amande effilee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Amande effilée', 'amande effilee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15041:amande effilee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 593.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 25.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 8.76, 'g', 'estimated'),
      (v_profile, 'fat_g', 52.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 9.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Amande effilée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15041', to_jsonb('Amande, mondée, émondée ou blanchie'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'amande grillee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Amande grillée', 'amande grillee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Amande grillée', 'amande grillee',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15042:amande grillee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 604.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 24.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 8.72, 'g', 'estimated'),
      (v_profile, 'fat_g', 53.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 10.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Amande grillée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15042', to_jsonb('Amande, grillée, salée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'amande mondee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Amande mondée', 'amande mondee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Amande mondée', 'amande mondee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15041:amande mondee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 593.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 25.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 8.76, 'g', 'estimated'),
      (v_profile, 'fat_g', 52.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 9.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Amande mondée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15041', to_jsonb('Amande, mondée, émondée ou blanchie'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ananas frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ananas frais', 'ananas frais', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ananas frais', 'ananas frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13999:ananas frais', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 59.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.26, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.97, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ananas frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13999', to_jsonb('Fruit cru (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'anchois a l huile egoutte' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Anchois à l''huile', 'anchois a l huile', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Anchois à l''huile, égoutté', 'anchois a l huile egoutte',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26000:anchois a l huile egoutte', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 182, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 26.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 8.45, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.25, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Anchois à l''huile, égoutté', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26000', to_jsonb('Anchois, filets à l''huile, semi-conserve, égoutté'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'aneth frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Aneth frais', 'aneth frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Aneth frais', 'aneth frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11093:aneth frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 48.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.93, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Aneth frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11093', to_jsonb('Aneth, frais'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'anis etoile' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Anis étoilé', 'anis etoile', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Anis étoilé', 'anis etoile',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '171316:anis etoile', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 337, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 17.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 50, 'g', 'estimated'),
      (v_profile, 'fat_g', 15.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 14.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 2, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Anis étoilé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '171316', to_jsonb('Spices, anise seed'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'aonori' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Aonori', 'aonori', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Aonori', 'aonori',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20995:aonori', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 224, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 18.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.47, 'g', 'estimated'),
      (v_profile, 'fiber_g', 36, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Aonori', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20995', to_jsonb('Ao-nori (Enteromorpha sp.), séchée ou déshydratée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'arachide grillee moulue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Arachide grillée moulue', 'arachide grillee moulue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Arachide grillée moulue', 'arachide grillee moulue',
       'powder', 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15053:arachide grillee moulue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 605.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 26.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 51.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 8.67, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Arachide grillée moulue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15053', to_jsonb('Cacahuète, grillée, sans sel ajouté'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'aubergine africaine fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Aubergine africaine fraîche', 'aubergine africaine fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Aubergine africaine fraîche', 'aubergine africaine fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20053:aubergine africaine fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 15.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.39, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Aubergine africaine fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20053', to_jsonb('Aubergine, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'aubergine chinoise fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Aubergine chinoise fraîche', 'aubergine chinoise fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Aubergine chinoise fraîche', 'aubergine chinoise fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20053:aubergine chinoise fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 15.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.39, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Aubergine chinoise fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20053', to_jsonb('Aubergine, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'aubergine fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Aubergine fraîche', 'aubergine fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Aubergine fraîche', 'aubergine fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20053:aubergine fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 15.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.39, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 250, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Aubergine fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20053', to_jsonb('Aubergine, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'aubergine japonaise fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Aubergine japonaise fraîche', 'aubergine japonaise fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Aubergine japonaise fraîche', 'aubergine japonaise fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20053:aubergine japonaise fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 15.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.39, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Aubergine japonaise fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20053', to_jsonb('Aubergine, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'aubergine thai fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Aubergine thaï fraîche', 'aubergine thai fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Aubergine thaï fraîche', 'aubergine thai fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20053:aubergine thai fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 15.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.39, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Aubergine thaï fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20053', to_jsonb('Aubergine, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'avocat frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Avocat frais', 'avocat frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Avocat frais', 'avocat frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13004:avocat frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 205, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.56, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.83, 'g', 'estimated'),
      (v_profile, 'fat_g', 20.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 140, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Avocat frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13004', to_jsonb('Avocat, pulpe, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'baguette vietnamienne' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Baguette vietnamienne', 'baguette vietnamienne', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Baguette vietnamienne', 'baguette vietnamienne',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7001:baguette vietnamienne', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 284, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.06, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 58.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Baguette vietnamienne', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7001', to_jsonb('Pain, baguette, courante'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'baie de genievre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Baie de genièvre', 'baie de genievre', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Baie de genièvre', 'baie de genievre',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11081:baie de genievre', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 345, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 37.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 10.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 25.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 0.1, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Baie de genièvre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11081', to_jsonb('Epice (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'banane mure' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Banane mûre', 'banane mure', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Banane mûre', 'banane mure',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13005:banane mure', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 90.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.06, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 19.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Banane mûre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13005', to_jsonb('Banane, pulpe, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'banane plantain mure' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Banane plantain mûre', 'banane plantain mure', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Banane plantain mûre', 'banane plantain mure',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '53100:banane plantain mure', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 127, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.28, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 29.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.39, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 180, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Banane plantain mûre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '53100', to_jsonb('Banane plantain, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'banane plantain verte' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Banane plantain verte', 'banane plantain verte', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Banane plantain verte', 'banane plantain verte',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '53100:banane plantain verte', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 127, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.28, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 29.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.39, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 180, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Banane plantain verte', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '53100', to_jsonb('Banane plantain, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bar entier cru vide' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bar entier cru vidé', 'bar entier cru vide', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bar entier cru vidé', 'bar entier cru vide',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26250:bar entier cru vide', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 106, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bar entier cru vidé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26250', to_jsonb('Poisson blanc, de mer, cuit (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'basilic frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Basilic frais', 'basilic frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Basilic frais', 'basilic frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11033:basilic frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 34.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.35, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.55, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.47, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.47, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Basilic frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11033', to_jsonb('Basilic, frais'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'basilic sacre frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Basilic sacré frais', 'basilic sacre frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Basilic sacré frais', 'basilic sacre frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11033:basilic sacre frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 34.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.35, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.55, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.47, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.47, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Basilic sacré frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11033', to_jsonb('Basilic, frais'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'basilic thai frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Basilic thaï frais', 'basilic thai frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Basilic thaï frais', 'basilic thai frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11033:basilic thai frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 34.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.35, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.55, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.47, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.47, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Basilic thaï frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11033', to_jsonb('Basilic, frais'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'baton de cannelle' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bâton de cannelle', 'baton de cannelle', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bâton de cannelle', 'baton de cannelle',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11025:baton de cannelle', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 243, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 27.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.22, 'g', 'estimated'),
      (v_profile, 'fiber_g', 53.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 4, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bâton de cannelle', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11025', to_jsonb('Cannelle, poudre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bechamel preparee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Béchamel préparée', 'bechamel preparee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Béchamel préparée', 'bechamel preparee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11143:bechamel preparee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 146.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.84, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 8.97, 'g', 'estimated'),
      (v_profile, 'fat_g', 10.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Béchamel préparée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11143', to_jsonb('Sauce béchamel, maison'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'berbere' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Berbéré', 'berbere', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Berbéré', 'berbere',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11005:berbere', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 301, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 53.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Berbéré', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11005', to_jsonb('Curry, poudre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'betterave crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Betterave crue', 'betterave crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Betterave crue', 'betterave crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20091:betterave crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.74, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 9.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.24, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.55, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Betterave crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20091', to_jsonb('Betterave rouge, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'beurre clarifie' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Beurre clarifié', 'beurre clarifie', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Beurre clarifié', 'beurre clarifie',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '16401:beurre clarifie', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.27, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Beurre clarifié', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '16401', to_jsonb('Huile de beurre ou Beurre concentré'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'beurre clarifie epice niter kibbeh' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Beurre clarifié épicé niter kibbeh', 'beurre clarifie epice niter kibbeh', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Beurre clarifié épicé niter kibbeh', 'beurre clarifie epice niter kibbeh',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '16401:beurre clarifie epice niter kibbeh', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.27, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Beurre clarifié épicé niter kibbeh', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '16401', to_jsonb('Huile de beurre ou Beurre concentré'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'beurre clarifie ghee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Beurre clarifié ghee', 'beurre clarifie ghee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Beurre clarifié ghee', 'beurre clarifie ghee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '16401:beurre clarifie ghee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.27, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Beurre clarifié ghee', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '16401', to_jsonb('Huile de beurre ou Beurre concentré'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'beurre demi sel' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Beurre demi-sel', 'beurre demi sel', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Beurre demi-sel', 'beurre demi sel',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '16402:beurre demi sel', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 738, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.72, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.55, 'g', 'estimated'),
      (v_profile, 'fat_g', 81.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Beurre demi-sel', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '16402', to_jsonb('Beurre à 80% MG, demi-sel'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'beurre doux' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Beurre doux', 'beurre doux', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Beurre doux', 'beurre doux',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '16400:beurre doux', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 753, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.69, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 82.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Beurre doux', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '16400', to_jsonb('Beurre à 82% MG, doux'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bicarbonate de sodium alimentaire' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bicarbonate de sodium alimentaire', 'bicarbonate de sodium alimentaire', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bicarbonate de sodium alimentaire', 'bicarbonate de sodium alimentaire',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11507:bicarbonate de sodium alimentaire', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 0.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bicarbonate de sodium alimentaire', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11507', to_jsonb('Bicarbonate de soude'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'biere blonde' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bière blonde', 'biere blonde', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bière blonde', 'biere blonde',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '5001:biere blonde', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 12.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.39, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bière blonde', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '5001', to_jsonb('Bière "coeur de marché" (4-5° alcool)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'biere blonde froide' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bière blonde froide', 'biere blonde froide', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bière blonde froide', 'biere blonde froide',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '5001:biere blonde froide', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 12.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.39, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bière blonde froide', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '5001', to_jsonb('Bière "coeur de marché" (4-5° alcool)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'biere brune belge' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bière brune belge', 'biere brune belge', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bière brune belge', 'biere brune belge',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '5000:biere brune belge', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 43, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.43, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bière brune belge', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '5000', to_jsonb('Bière brune'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'biscuit cuillere' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Biscuit cuillère', 'biscuit cuillere', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Biscuit cuillère', 'biscuit cuillere',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '24430:biscuit cuillere', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 385.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.98, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 79.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.77, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.37, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Biscuit cuillère', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '24430', to_jsonb('Biscuit sec aux oeufs à la cuillère (cuiller) ou Boudoir'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'oeufs';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'blanc d oeuf cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Blanc d''œuf cru', 'blanc d oeuf cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Blanc d''œuf cru', 'blanc d oeuf cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '22001:blanc d oeuf cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 48.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 10.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.85, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.19, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 33, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Blanc d''œuf cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '22001', to_jsonb('Oeuf, blanc (blanc d''oeuf), cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'volailles';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'blanc de poulet cru sans peau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Blanc de poulet cru', 'blanc de poulet cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Blanc de poulet cru, sans peau', 'blanc de poulet cru sans peau',
       NULL, 'raw', NULL,
       NULL, 'skinless',
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36017:blanc de poulet cru sans peau', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 110, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 23.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Blanc de poulet cru, sans peau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36017', to_jsonb('Poulet, filet, sans peau, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ble concasse' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Blé concassé', 'ble concasse', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Blé concassé', 'ble concasse',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9690:ble concasse', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 348, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 66.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.71, 'g', 'estimated'),
      (v_profile, 'fiber_g', 9.57, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Blé concassé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9690', to_jsonb('Boulgour de blé, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf a braiser cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf à braiser cru', 'boeuf a braiser cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf à braiser cru', 'boeuf a braiser cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6231:boeuf a braiser cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 164, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 24, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 7.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf à braiser cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6231', to_jsonb('Boeuf, à bourguignon ou pot-au-feu, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf a braiser cru en petits cubes' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf à braiser cru', 'boeuf a braiser cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf à braiser cru, en petits cubes', 'boeuf a braiser cru en petits cubes',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6231:boeuf a braiser cru en petits cubes', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 164, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 24, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 7.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf à braiser cru, en petits cubes', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6231', to_jsonb('Boeuf, à bourguignon ou pot-au-feu, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf a griller cru en cubes' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf à griller cru', 'boeuf a griller cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf à griller cru, en cubes', 'boeuf a griller cru en cubes',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6206:boeuf a griller cru en cubes', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 114, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf à griller cru, en cubes', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6206', to_jsonb('Boeuf, rumsteck, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf a griller cru en fines lamelles' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf à griller cru', 'boeuf a griller cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf à griller cru, en fines lamelles', 'boeuf a griller cru en fines lamelles',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6111:boeuf a griller cru en fines lamelles', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 152, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.74, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf à griller cru, en fines lamelles', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6111', to_jsonb('Boeuf, faux-filet, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf cru finement tranche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf cru finement tranché', 'boeuf cru finement tranche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf cru finement tranché', 'boeuf cru finement tranche',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6160:boeuf cru finement tranche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 116, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 23.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.57, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.34, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf cru finement tranché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6160', to_jsonb('Boeuf, tende de tranche, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf cru tres finement tranche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf cru très finement tranché', 'boeuf cru tres finement tranche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf cru très finement tranché', 'boeuf cru tres finement tranche',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6111:boeuf cru tres finement tranche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 152, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.74, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf cru très finement tranché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6111', to_jsonb('Boeuf, faux-filet, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf cuit effiloche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf cuit effiloché', 'boeuf cuit effiloche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf cuit effiloché', 'boeuf cuit effiloche',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6271:boeuf cuit effiloche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 251, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 36.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 11.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf cuit effiloché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6271', to_jsonb('Boeuf, paleron, braisé ou bouilli'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf hache au couteau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf haché au couteau', 'boeuf hache au couteau', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf haché au couteau', 'boeuf hache au couteau',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6254:boeuf hache au couteau', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 209, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.47, 'g', 'estimated'),
      (v_profile, 'fat_g', 14.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf haché au couteau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6254', to_jsonb('Boeuf, steak haché 15% MG, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf hache cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf haché cru', 'boeuf hache cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf haché cru', 'boeuf hache cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6254:boeuf hache cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 209, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.47, 'g', 'estimated'),
      (v_profile, 'fat_g', 14.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf haché cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6254', to_jsonb('Boeuf, steak haché 15% MG, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf hache cru 15 mg' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf haché cru 15 % MG', 'boeuf hache cru 15 mg', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf haché cru 15 % MG', 'boeuf hache cru 15 mg',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6254:boeuf hache cru 15 mg', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 209, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.47, 'g', 'estimated'),
      (v_profile, 'fat_g', 14.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf haché cru 15 % MG', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6254', to_jsonb('Boeuf, steak haché 15% MG, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf marine bulgogi cuit' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf mariné bulgogi cuit', 'boeuf marine bulgogi cuit', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf mariné bulgogi cuit', 'boeuf marine bulgogi cuit',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6100:boeuf marine bulgogi cuit', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 198, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 25.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 10.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf mariné bulgogi cuit', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6100', to_jsonb('Boeuf, entrecôte, partie maigre, grillée/poêlée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boeuf persille cru tres fin' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bœuf persillé cru très fin', 'boeuf persille cru tres fin', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bœuf persillé cru très fin', 'boeuf persille cru tres fin',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6111:boeuf persille cru tres fin', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 152, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.74, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bœuf persillé cru très fin', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6111', to_jsonb('Boeuf, faux-filet, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boudin noir a cuire' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Boudin noir à cuire', 'boudin noir a cuire', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Boudin noir à cuire', 'boudin noir a cuire',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '8703:boudin noir a cuire', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 313, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 27.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.42, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Boudin noir à cuire', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '8703', to_jsonb('Boudin noir, à cuire'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon d agneau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon d’agneau', 'bouillon d agneau', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon d’agneau', 'bouillon d agneau',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25930:bouillon d agneau', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 9.05, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.33, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.15, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon d’agneau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25930', to_jsonb('Bouillon de boeuf, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon d agneau non sale' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon d’agneau non salé', 'bouillon d agneau non sale', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon d’agneau non salé', 'bouillon d agneau non sale',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25909:bouillon d agneau non sale', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 4.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon d’agneau non salé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25909', to_jsonb('Bouillon de viande et légumes type pot-au-feu, prêt à consommer'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon d anchois' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon d’anchois', 'bouillon d anchois', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon d’anchois', 'bouillon d anchois',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25909:bouillon d anchois', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 4.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon d’anchois', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25909', to_jsonb('Bouillon de viande et légumes type pot-au-feu, prêt à consommer'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon d anchois et algue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon d’anchois et algue', 'bouillon d anchois et algue', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon d’anchois et algue', 'bouillon d anchois et algue',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25948:bouillon d anchois et algue', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 5.38, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.34, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.65, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.16, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon d’anchois et algue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25948', to_jsonb('Bouillon de légumes, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de boeuf' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de bœuf', 'bouillon de boeuf', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de bœuf', 'bouillon de boeuf',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25930:bouillon de boeuf', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 9.05, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.33, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.15, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de bœuf', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25930', to_jsonb('Bouillon de boeuf, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de boeuf non sale' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de bœuf non salé', 'bouillon de boeuf non sale', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de bœuf non salé', 'bouillon de boeuf non sale',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25930:bouillon de boeuf non sale', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 9.05, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.33, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.15, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de bœuf non salé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25930', to_jsonb('Bouillon de boeuf, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de crevette' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de crevette', 'bouillon de crevette', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de crevette', 'bouillon de crevette',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25919:bouillon de crevette', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 29, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.91, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5.09, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.21, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de crevette', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25919', to_jsonb('Soupe de poissons et / ou crustacés, déshydratée reconstituée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de legumes' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de légumes', 'bouillon de legumes', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de légumes', 'bouillon de legumes',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25948:bouillon de legumes', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 5.38, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.34, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.65, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.16, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de légumes', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25948', to_jsonb('Bouillon de légumes, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de legumes non sale' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de légumes non salé', 'bouillon de legumes non sale', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de légumes non salé', 'bouillon de legumes non sale',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25948:bouillon de legumes non sale', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 5.38, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.34, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.65, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.16, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de légumes non salé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25948', to_jsonb('Bouillon de légumes, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de porc et poulet' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de porc et poulet', 'bouillon de porc et poulet', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de porc et poulet', 'bouillon de porc et poulet',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25947:bouillon de porc et poulet', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 5.58, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.13, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.0001, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de porc et poulet', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25947', to_jsonb('Bouillon de volaille, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de poulet' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de poulet', 'bouillon de poulet', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de poulet', 'bouillon de poulet',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25947:bouillon de poulet', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 5.58, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.13, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.0001, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de poulet', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25947', to_jsonb('Bouillon de volaille, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de veau non sale' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de veau non salé', 'bouillon de veau non sale', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de veau non salé', 'bouillon de veau non sale',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25930:bouillon de veau non sale', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 9.05, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.33, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.15, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de veau non salé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25930', to_jsonb('Bouillon de boeuf, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de volaille' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de volaille', 'bouillon de volaille', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de volaille', 'bouillon de volaille',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25947:bouillon de volaille', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 5.58, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.13, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.0001, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de volaille', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25947', to_jsonb('Bouillon de volaille, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de volaille clair' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de volaille clair', 'bouillon de volaille clair', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de volaille clair', 'bouillon de volaille clair',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25947:bouillon de volaille clair', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 5.58, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.13, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.0001, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de volaille clair', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25947', to_jsonb('Bouillon de volaille, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouillon de volaille non sale' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouillon de volaille non salé', 'bouillon de volaille non sale', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouillon de volaille non salé', 'bouillon de volaille non sale',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25947:bouillon de volaille non sale', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 5.58, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.13, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.0001, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouillon de volaille non salé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25947', to_jsonb('Bouillon de volaille, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'boulgour fin cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Boulgour fin cru', 'boulgour fin cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Boulgour fin cru', 'boulgour fin cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9690:boulgour fin cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 348, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 66.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.71, 'g', 'estimated'),
      (v_profile, 'fiber_g', 9.57, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Boulgour fin cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9690', to_jsonb('Boulgour de blé, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bouquet garni frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bouquet garni frais', 'bouquet garni frais', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bouquet garni frais', 'bouquet garni frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'u', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11014:bouquet garni frais', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 43, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.71, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.48, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.63, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 15, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bouquet garni frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11014', to_jsonb('Persil, frais'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'brocoli cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Brocoli cru', 'brocoli cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Brocoli cru', 'brocoli cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20057:brocoli cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 26.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.95, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.48, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Brocoli cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20057', to_jsonb('Brocoli, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'bucatini secs' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Bucatini secs', 'bucatini secs', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Bucatini secs', 'bucatini secs',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9810:bucatini secs', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 331, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 65.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.79, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Bucatini secs', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9810', to_jsonb('Pâtes sèches standard, crues'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cacahuete grillee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cacahuète grillée', 'cacahuete grillee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cacahuète grillée', 'cacahuete grillee',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15053:cacahuete grillee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 605.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 26.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 51.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 8.67, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cacahuète grillée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15053', to_jsonb('Cacahuète, grillée, sans sel ajouté'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cacao non sucre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cacao non sucré', 'cacao non sucre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cacao non sucré', 'cacao non sucre',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '18100:cacao non sucre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 387, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 20.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 29.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cacao non sucré', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '18100', to_jsonb('Cacao, non sucré, poudre soluble'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cafe expresso refroidi' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Café expresso refroidi', 'cafe expresso refroidi', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Café expresso refroidi', 'cafe expresso refroidi',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '18071:cafe expresso refroidi', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 7.64, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.16, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Café expresso refroidi', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '18071', to_jsonb('Café expresso, non instantané, non sucré, prêt à boire'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cafe moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Café moulu', 'cafe moulu', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Café moulu', 'cafe moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '18003:cafe moulu', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 397, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 40.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 15.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 19.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Café moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '18003', to_jsonb('Café, moulu'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'calamar cru nettoye' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Calamar cru nettoyé', 'calamar cru nettoye', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Calamar cru nettoyé', 'calamar cru nettoye',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '10001:calamar cru nettoye', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 77, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.17, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.19, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Calamar cru nettoyé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '10001', to_jsonb('Calmar ou calamar ou encornet, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'calvados' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Calvados', 'calvados', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Calvados', 'calvados',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '1023:calvados', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 228, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.95, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Calvados', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '1023', to_jsonb('Eau de vie de vin, type armagnac, cognac'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cannelle cassia' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cannelle cassia', 'cannelle cassia', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cannelle cassia', 'cannelle cassia',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11025:cannelle cassia', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 243, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 27.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.22, 'g', 'estimated'),
      (v_profile, 'fiber_g', 53.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 4, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cannelle cassia', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11025', to_jsonb('Cannelle, poudre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cannelle en baton' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cannelle en bâton', 'cannelle en baton', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cannelle en bâton', 'cannelle en baton',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11025:cannelle en baton', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 243, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 27.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.22, 'g', 'estimated'),
      (v_profile, 'fiber_g', 53.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 4, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cannelle en bâton', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11025', to_jsonb('Cannelle, poudre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cannelle moulue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cannelle moulue', 'cannelle moulue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cannelle moulue', 'cannelle moulue',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11025:cannelle moulue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 243, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 27.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.22, 'g', 'estimated'),
      (v_profile, 'fiber_g', 53.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cannelle moulue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11025', to_jsonb('Cannelle, poudre'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'capre au vinaigre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Câpre au vinaigre', 'capre au vinaigre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Câpre au vinaigre', 'capre au vinaigre',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11040:capre au vinaigre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 30.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.18, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.86, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Câpre au vinaigre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11040', to_jsonb('Câpres, au vinaigre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'capre au vinaigre egouttee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Câpre au vinaigre', 'capre au vinaigre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Câpre au vinaigre, égouttée', 'capre au vinaigre egouttee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11040:capre au vinaigre egouttee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 30.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.18, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.86, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Câpre au vinaigre, égouttée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11040', to_jsonb('Câpres, au vinaigre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cardamome noire' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cardamome noire', 'cardamome noire', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cardamome noire', 'cardamome noire',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11075:cardamome noire', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 321, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 10.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 40.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 28, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 1, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cardamome noire', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11075', to_jsonb('Cardamome, poudre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cardamome verte' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cardamome verte', 'cardamome verte', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cardamome verte', 'cardamome verte',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11075:cardamome verte', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 321, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 10.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 40.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 28, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 0.2, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cardamome verte', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11075', to_jsonb('Cardamome, poudre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'carotte crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Carotte crue', 'carotte crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Carotte crue', 'carotte crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20009:carotte crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 40.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.63, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.59, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Carotte crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20009', to_jsonb('Carotte, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'carotte nouvelle crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Carotte nouvelle crue', 'carotte nouvelle crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Carotte nouvelle crue', 'carotte nouvelle crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20009:carotte nouvelle crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 40.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.63, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.59, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Carotte nouvelle crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20009', to_jsonb('Carotte, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'carvi en graines' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Carvi en graines', 'carvi en graines', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Carvi en graines', 'carvi en graines',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11064:carvi en graines', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 334, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 14.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 38, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Carvi en graines', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11064', to_jsonb('Carvi, graine'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cassonade' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cassonade', 'cassonade', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cassonade', 'cassonade',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31017:cassonade', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 389.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 97.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cassonade', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31017', to_jsonb('Sucre roux'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'celeri branche cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Céleri branche cru', 'celeri branche cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Céleri branche cru', 'celeri branche cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20023:celeri branche cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 17.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.63, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.41, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Céleri branche cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20023', to_jsonb('Céleri branche, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cerfeuil frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cerfeuil frais', 'cerfeuil frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cerfeuil frais', 'cerfeuil frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11002:cerfeuil frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 39.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.72, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.55, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cerfeuil frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11002', to_jsonb('Cerfeuil, frais'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cerise fraiche denoyautee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cerise fraîche dénoyautée', 'cerise fraiche denoyautee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cerise fraîche dénoyautée', 'cerise fraiche denoyautee',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13008:cerise fraiche denoyautee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 55.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.81, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 13, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cerise fraîche dénoyautée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13008', to_jsonb('Cerise, dénoyautée, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chair de brochet crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chair de brochet crue', 'chair de brochet crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chair de brochet crue', 'chair de brochet crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '27011:chair de brochet crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 83.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 18.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.01, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.94, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chair de brochet crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '27011', to_jsonb('Brochet, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'champignon de paille' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Champignon de paille', 'champignon de paille', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Champignon de paille', 'champignon de paille',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20011:champignon de paille', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 24.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.56, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.29, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Champignon de paille', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20011', to_jsonb('Champignon, tout type, appertisé, égoutté'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'champignon de paris frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Champignon de Paris frais', 'champignon de paris frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Champignon de Paris frais', 'champignon de paris frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20056:champignon de paris frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 28, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.62, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.15, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.36, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Champignon de Paris frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20056', to_jsonb('Champignon de Paris ou champignon de couche, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'champignon seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Champignon séché', 'champignon seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Champignon séché', 'champignon seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20211:champignon seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 316, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.58, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 63.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.99, 'g', 'estimated'),
      (v_profile, 'fiber_g', 11.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Champignon séché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20211', to_jsonb('Champignon, lentin comestible ou shiitaké, séché'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chana masala' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chana masala', 'chana masala', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chana masala', 'chana masala',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20507:chana masala', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 147, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 8.31, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 17.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 8.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chana masala', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20507', to_jsonb('Pois chiche, bouilli/cuit à l''eau'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chapelure de ble' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chapelure de blé', 'chapelure de ble', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chapelure de blé', 'chapelure de ble',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7500:chapelure de ble', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 369, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 70, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.77, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chapelure de blé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7500', to_jsonb('Chapelure'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chapelure fine de ble' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chapelure fine de blé', 'chapelure fine de ble', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chapelure fine de blé', 'chapelure fine de ble',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7500:chapelure fine de ble', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 369, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 70, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.77, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chapelure fine de blé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7500', to_jsonb('Chapelure'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chashu de porc' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chashu de porc', 'chashu de porc', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chashu de porc', 'chashu de porc',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28401:chashu de porc', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 333, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 23.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.13, 'g', 'estimated'),
      (v_profile, 'fat_g', 26.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chashu de porc', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28401', to_jsonb('Porc, travers, braisé'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chataigne d eau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Châtaigne d’eau', 'chataigne d eau', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Châtaigne d’eau', 'chataigne d eau',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20196:chataigne d eau', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 60.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.94, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.31, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Châtaigne d’eau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20196', to_jsonb('Topinambour, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chicharron de porc' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chicharrón de porc', 'chicharron de porc', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chicharrón de porc', 'chicharron de porc',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '8001:chicharron de porc', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 414, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 16.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.13, 'g', 'estimated'),
      (v_profile, 'fat_g', 38.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chicharrón de porc', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '8001', to_jsonb('Rillettes pur porc'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chocolat noir 70' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chocolat noir 70 %', 'chocolat noir 70', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chocolat noir 70 %', 'chocolat noir 70',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31074:chocolat noir 70', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 591, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 10.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 26.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 46.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 12.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chocolat noir 70 %', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31074', to_jsonb('Chocolat noir à 70% cacao minimum, extra, dégustation, tablette'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chocolat noir mexicain' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chocolat noir mexicain', 'chocolat noir mexicain', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chocolat noir mexicain', 'chocolat noir mexicain',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31085:chocolat noir mexicain', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 529.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.39, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 51.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 33.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 8.76, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chocolat noir mexicain', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31085', to_jsonb('Chocolat noir à 40% de cacao minimum, à pâtisser, tablette'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chorizo asturien' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chorizo asturien', 'chorizo asturien', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chorizo asturien', 'chorizo asturien',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '30315:chorizo asturien', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 433, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 23.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.05, 'g', 'estimated'),
      (v_profile, 'fat_g', 36.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.93, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chorizo asturien', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '30315', to_jsonb('Chorizo'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chorizo criollo' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chorizo criollo', 'chorizo criollo', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chorizo criollo', 'chorizo criollo',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '30110:chorizo criollo', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 327, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1, 'g', 'estimated'),
      (v_profile, 'fat_g', 30, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.64, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chorizo criollo', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '30110', to_jsonb('Saucisse de Toulouse, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou blanc blanchi' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou blanc blanchi', 'chou blanc blanchi', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou blanc blanchi', 'chou blanc blanchi',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20308:chou blanc blanchi', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 23.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.23, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou blanc blanchi', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20308', to_jsonb('Chou blanc, bouilli/cuit à l''eau'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou blanc fermente curtido' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou blanc fermenté curtido', 'chou blanc fermente curtido', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou blanc fermenté curtido', 'chou blanc fermente curtido',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20116:chou blanc fermente curtido', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 36.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.38, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou blanc fermenté curtido', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20116', to_jsonb('Chou blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou blanc frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou blanc frais', 'chou blanc frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou blanc frais', 'chou blanc frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20116:chou blanc frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 36.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.38, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou blanc frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20116', to_jsonb('Chou blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou chinois frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou chinois frais', 'chou chinois frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou chinois frais', 'chou chinois frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20167:chou chinois frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 13.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.38, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.65, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou chinois frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20167', to_jsonb('Chou chinois ou pak-choi ou pé-tsai, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou chinois sale' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou chinois salé', 'chou chinois sale', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou chinois salé', 'chou chinois sale',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20167:chou chinois sale', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 13.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.38, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.65, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou chinois salé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20167', to_jsonb('Chou chinois ou pak-choi ou pé-tsai, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou emince frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou émincé frais', 'chou emince frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou émincé frais', 'chou emince frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20116:chou emince frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 36.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.38, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou émincé frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20116', to_jsonb('Chou blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou kale frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou kale frais', 'chou kale frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou kale frais', 'chou kale frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20218:chou kale frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 43.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4.33, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.07, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou kale frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20218', to_jsonb('Chou frisé, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou kale portugais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou kale portugais', 'chou kale portugais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou kale portugais', 'chou kale portugais',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20218:chou kale portugais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 43.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4.33, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.07, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou kale portugais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20218', to_jsonb('Chou frisé, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou noir kale' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou noir kale', 'chou noir kale', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou noir kale', 'chou noir kale',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20218:chou noir kale', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 43.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4.33, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.07, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou noir kale', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20218', to_jsonb('Chou frisé, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou vert frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou vert frais', 'chou vert frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou vert frais', 'chou vert frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20069:chou vert frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 21.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.53, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.92, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.41, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.08, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou vert frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20069', to_jsonb('Chou vert, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chou fleur frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chou-fleur frais', 'chou fleur frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chou-fleur frais', 'chou fleur frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20016:chou fleur frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 26.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.81, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.13, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chou-fleur frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20016', to_jsonb('Chou-fleur, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'choucroute crue fermentee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Choucroute crue fermentée', 'choucroute crue fermentee', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Choucroute crue fermentée', 'choucroute crue fermentee',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20116:choucroute crue fermentee', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 36.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.38, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Choucroute crue fermentée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20116', to_jsonb('Chou blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chourico portugais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chouriço portugais', 'chourico portugais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chouriço portugais', 'chourico portugais',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '173859:chourico portugais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 296, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.78, 'g', 'estimated'),
      (v_profile, 'fat_g', 25.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chouriço portugais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '173859', to_jsonb('Sausage, pork, chorizo, link or ground, raw'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'chutney d abricot' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Chutney d abricot', 'chutney d abricot', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Chutney d abricot', 'chutney d abricot',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31037:chutney d abricot', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 247, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 59.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Chutney d abricot', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31037', to_jsonb('Confiture d''abricot (extra ou classique)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ciboule fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ciboule fraîche', 'ciboule fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ciboule fraîche', 'ciboule fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11003:ciboule fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 29.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.57, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.52, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.19, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ciboule fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11003', to_jsonb('Ciboule ou Ciboulette, fraîche'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ciboulette chinoise fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ciboulette chinoise fraîche', 'ciboulette chinoise fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ciboulette chinoise fraîche', 'ciboulette chinoise fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11003:ciboulette chinoise fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 29.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.57, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.52, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.19, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ciboulette chinoise fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11003', to_jsonb('Ciboule ou Ciboulette, fraîche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ciboulette fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ciboulette fraîche', 'ciboulette fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ciboulette fraîche', 'ciboulette fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11003:ciboulette fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 29.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.57, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.52, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.19, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ciboulette fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11003', to_jsonb('Ciboule ou Ciboulette, fraîche'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cidre brut' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cidre brut', 'cidre brut', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cidre brut', 'cidre brut',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '5006:cidre brut', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 10.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cidre brut', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '5006', to_jsonb('Cidre brut'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cinq epices chinoises' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cinq épices chinoises', 'cinq epices chinoises', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cinq épices chinoises', 'cinq epices chinoises',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11081:cinq epices chinoises', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 345, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 37.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 10.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 25.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cinq épices chinoises', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11081', to_jsonb('Epice (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'citron confit au sel' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Citron confit au sel', 'citron confit au sel', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Citron confit au sel', 'citron confit au sel',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13009:citron confit au sel', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 27.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.56, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 70, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Citron confit au sel', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13009', to_jsonb('Citron, pulpe, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'citron jaune frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Citron jaune frais', 'citron jaune frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Citron jaune frais', 'citron jaune frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13009:citron jaune frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 27.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.56, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 120, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Citron jaune frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13009', to_jsonb('Citron, pulpe, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'citron vert frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Citron vert frais', 'citron vert frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Citron vert frais', 'citron vert frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13067:citron vert frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 40.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.13, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.14, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 35, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Citron vert frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13067', to_jsonb('Citron vert ou Lime, pulpe, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'citronnelle fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Citronnelle fraîche', 'citronnelle fraiche', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Citronnelle fraîche', 'citronnelle fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11080:citronnelle fraiche', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 49.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.58, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.15, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.64, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Citronnelle fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11080', to_jsonb('Herbes aromatiques fraîches (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'clou de girofle' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Clou de girofle', 'clou de girofle', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Clou de girofle', 'clou de girofle',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11052:clou de girofle', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 335, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 5.97, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 31.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 13, 'g', 'estimated'),
      (v_profile, 'fiber_g', 33.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 0.2, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Clou de girofle', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11052', to_jsonb('Clou de girofle'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'clou de girofle moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Clou de girofle moulu', 'clou de girofle moulu', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Clou de girofle moulu', 'clou de girofle moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11052:clou de girofle moulu', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 335, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 5.97, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 31.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 13, 'g', 'estimated'),
      (v_profile, 'fiber_g', 33.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Clou de girofle moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11052', to_jsonb('Clou de girofle'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cognac' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cognac', 'cognac', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cognac', 'cognac',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '1023:cognac', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 228, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.95, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cognac', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '1023', to_jsonb('Eau de vie de vin, type armagnac, cognac'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'comte' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Comté', 'comte', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Comté', 'comte',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '12110:comte', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 420, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 26.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.36, 'g', 'estimated'),
      (v_profile, 'fat_g', 34.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Comté', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '12110', to_jsonb('Comté'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'concentre de tomate' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Concentré de tomate', 'concentre de tomate', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Concentré de tomate', 'concentre de tomate',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20068:concentre de tomate', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 99.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 17.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.53, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Concentré de tomate', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20068', to_jsonb('Tomate, concentré, appertisé'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'concombre frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Concombre frais', 'concombre frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Concombre frais', 'concombre frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20019:concombre frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 15.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.64, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.54, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.11, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Concombre frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20019', to_jsonb('Concombre, pulpe et peau, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'confit de canard cuit' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Confit de canard cuit', 'confit de canard cuit', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Confit de canard cuit', 'confit de canard cuit',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '8110:confit de canard cuit', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 273, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 25.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.15, 'g', 'estimated'),
      (v_profile, 'fat_g', 18.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Confit de canard cuit', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '8110', to_jsonb('Confit de canard'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'confit de canard cuit effiloche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Confit de canard cuit', 'confit de canard cuit', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Confit de canard cuit, effiloché', 'confit de canard cuit effiloche',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '8110:confit de canard cuit effiloche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 273, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 25.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.15, 'g', 'estimated'),
      (v_profile, 'fat_g', 18.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Confit de canard cuit, effiloché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '8110', to_jsonb('Confit de canard'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'congre cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Congre cru', 'congre cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Congre cru', 'congre cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26127:congre cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 115, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 18.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Congre cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26127', to_jsonb('Congre, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'coriandre en graines' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Coriandre en graines', 'coriandre en graines', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Coriandre en graines', 'coriandre en graines',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11026:coriandre en graines', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 346, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 13, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 41.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Coriandre en graines', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11026', to_jsonb('Coriandre, graine'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'coriandre fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Coriandre fraîche', 'coriandre fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Coriandre fraîche', 'coriandre fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11094:coriandre fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.13, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.87, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.52, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Coriandre fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11094', to_jsonb('Coriandre, fraiche'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'coriandre moulue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Coriandre moulue', 'coriandre moulue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Coriandre moulue', 'coriandre moulue',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11026:coriandre moulue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 346, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 13, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 41.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Coriandre moulue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11026', to_jsonb('Coriandre, graine'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cornichon au vinaigre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cornichon au vinaigre', 'cornichon au vinaigre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cornichon au vinaigre', 'cornichon au vinaigre',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11004:cornichon au vinaigre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 19.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.06, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.78, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cornichon au vinaigre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11004', to_jsonb('Cornichon, au vinaigre'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'courge fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Courge fraîche', 'courge fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Courge fraîche', 'courge fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20139:courge fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 12.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.17, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Courge fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20139', to_jsonb('Courge, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'courgette fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Courgette fraîche', 'courgette fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Courgette fraîche', 'courgette fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20020:courgette fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 16.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.23, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.26, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Courgette fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20020', to_jsonb('Courgette, pulpe et peau, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'creme aigre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Crème aigre', 'creme aigre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Crème aigre', 'creme aigre',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19431:creme aigre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 166, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.75, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3, 'g', 'estimated'),
      (v_profile, 'fat_g', 15.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Crème aigre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19431', to_jsonb('Crème de lait, 15 à 20% MG, légère, épaisse, rayon frais'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'creme fraiche epaisse' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Crème fraîche épaisse', 'creme fraiche epaisse', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Crème fraîche épaisse', 'creme fraiche epaisse',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19410:creme fraiche epaisse', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 304, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.94, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 30.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Crème fraîche épaisse', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19410', to_jsonb('Crème de lait, 30% MG, épaisse, rayon frais'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'creme fraiche liquide entiere' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Crème fraîche liquide entière', 'creme fraiche liquide entiere', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Crème fraîche liquide entière', 'creme fraiche liquide entiere',
       'liquid', NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19415:creme fraiche liquide entiere', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 297, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.44, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.94, 'g', 'estimated'),
      (v_profile, 'fat_g', 30.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.03, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Crème fraîche liquide entière', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19415', to_jsonb('Crème de lait, 30% MG, semi-épaisse, UHT'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'creme mexicaine' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Crème mexicaine', 'creme mexicaine', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Crème mexicaine', 'creme mexicaine',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19402:creme mexicaine', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 269, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.82, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.87, 'g', 'estimated'),
      (v_profile, 'fat_g', 26, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.39, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Crème mexicaine', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19402', to_jsonb('Crème de lait ou spécialité à base de crème légère, teneur en matière grasse inconnue (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'crevette crue decortiquee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Crevette crue décortiquée', 'crevette crue decortiquee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Crevette crue décortiquée', 'crevette crue decortiquee',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '10021:crevette crue decortiquee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 99, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.21, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.84, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Crevette crue décortiquée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '10021', to_jsonb('Crevette, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'crevette crue entiere' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Crevette crue entière', 'crevette crue entiere', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Crevette crue entière', 'crevette crue entiere',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '10021:crevette crue entiere', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 99, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.21, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.84, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Crevette crue entière', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '10021', to_jsonb('Crevette, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'volailles';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cuisse de poulet crue avec os avec peau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cuisse de poulet crue', 'cuisse de poulet crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cuisse de poulet crue, avec os, avec peau', 'cuisse de poulet crue avec os avec peau',
       NULL, 'raw', NULL,
       'bone_in', 'with_skin',
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36002:cuisse de poulet crue avec os avec peau', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 192, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 17.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 13.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cuisse de poulet crue, avec os, avec peau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36002', to_jsonb('Poulet, cuisse, viande et peau, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'volailles';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cuisse de poulet crue avec os sans peau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cuisse de poulet crue', 'cuisse de poulet crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cuisse de poulet crue, avec os, sans peau', 'cuisse de poulet crue avec os sans peau',
       NULL, 'raw', NULL,
       'bone_in', 'skinless',
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36024:cuisse de poulet crue avec os sans peau', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 114, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.05, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cuisse de poulet crue, avec os, sans peau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36024', to_jsonb('Poulet, cuisse, viande, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cumin en graines' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cumin en graines', 'cumin en graines', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cumin en graines', 'cumin en graines',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11042:cumin en graines', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 427, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 17.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 33.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 22.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 10.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cumin en graines', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11042', to_jsonb('Cumin, graine'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'cumin moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Cumin moulu', 'cumin moulu', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Cumin moulu', 'cumin moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11042:cumin moulu', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 427, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 17.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 33.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 22.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 10.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Cumin moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11042', to_jsonb('Cumin, graine'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'curcuma moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Curcuma moulu', 'curcuma moulu', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Curcuma moulu', 'curcuma moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11089:curcuma moulu', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 291, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.68, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 44.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.25, 'g', 'estimated'),
      (v_profile, 'fiber_g', 22.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Curcuma moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11089', to_jsonb('Curcuma, poudre'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'curry en poudre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Curry en poudre', 'curry en poudre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Curry en poudre', 'curry en poudre',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11005:curry en poudre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 301, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 53.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Curry en poudre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11005', to_jsonb('Curry, poudre'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'dashi' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Dashi', 'dashi', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Dashi', 'dashi',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25948:dashi', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 5.38, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.34, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.65, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.16, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Dashi', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25948', to_jsonb('Bouillon de légumes, déshydraté reconstitué'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'doenjang pate de soja fermentee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Doenjang pâte de soja fermentée', 'doenjang pate de soja fermentee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Doenjang pâte de soja fermentée', 'doenjang pate de soja fermentee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20916:doenjang pate de soja fermentee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 163, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 11.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 18.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.31, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Doenjang pâte de soja fermentée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20916', to_jsonb('Miso'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'dos de cabillaud cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Dos de cabillaud cru', 'dos de cabillaud cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Dos de cabillaud cru', 'dos de cabillaud cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26043:dos de cabillaud cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 77.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 18.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.57, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Dos de cabillaud cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26043', to_jsonb('Cabillaud, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'doubanjiang' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Doubanjiang', 'doubanjiang', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Doubanjiang', 'doubanjiang',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20916:doubanjiang', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 163, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 11.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 18.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.31, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Doubanjiang', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20916', to_jsonb('Miso'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'douchi haricot noir fermente' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Douchi haricot noir fermenté', 'douchi haricot noir fermente', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Douchi haricot noir fermenté', 'douchi haricot noir fermente',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20917:douchi haricot noir fermente', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 151, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 17.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.89, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 6.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Douchi haricot noir fermenté', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20917', to_jsonb('Tempeh'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'eau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Eau', 'eau', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Eau', 'eau',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '18066:eau', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 0, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Eau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '18066', to_jsonb('Eau du robinet'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'eau chaude' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Eau chaude', 'eau chaude', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Eau chaude', 'eau chaude',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '18066:eau chaude', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 0, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Eau chaude', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '18066', to_jsonb('Eau du robinet'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'eau de coco' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Eau de coco', 'eau de coco', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Eau de coco', 'eau de coco',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '18011:eau de coco', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 15.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.33, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Eau de coco', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '18011', to_jsonb('Eau de coco'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'eau glacee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Eau glacée', 'eau glacee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Eau glacée', 'eau glacee',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '18066:eau glacee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 0, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Eau glacée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '18066', to_jsonb('Eau du robinet'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'eau glacee gazeuse' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Eau glacée gazeuse', 'eau glacee gazeuse', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Eau glacée gazeuse', 'eau glacee gazeuse',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '18046:eau glacee gazeuse', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 0, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Eau glacée gazeuse', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '18046', to_jsonb('Eau minérale, gazeuse (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'echalote crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Échalote crue', 'echalote crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Échalote crue', 'echalote crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20097:echalote crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 63.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.81, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 12.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Échalote crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20097', to_jsonb('Échalote, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'echine de porc crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Échine de porc crue', 'echine de porc crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Échine de porc crue', 'echine de porc crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28302:echine de porc crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 198, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 18, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Échine de porc crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28302', to_jsonb('Porc, échine, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'emmental rape' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Emmental râpé', 'emmental rape', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Emmental râpé', 'emmental rape',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '12118:emmental rape', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 370, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 27.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.65, 'g', 'estimated'),
      (v_profile, 'fat_g', 28.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Emmental râpé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '12118', to_jsonb('Emmental ou emmenthal râpé'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'endive fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Endive fraîche', 'endive fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Endive fraîche', 'endive fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20026:endive fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 20.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.19, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.83, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 150, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Endive fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20026', to_jsonb('Endive, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'eomuk gateau de poisson' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Eomuk gâteau de poisson', 'eomuk gateau de poisson', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Eomuk gâteau de poisson', 'eomuk gateau de poisson',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26046:eomuk gateau de poisson', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 125, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 8.31, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.92, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.33, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Eomuk gâteau de poisson', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26046', to_jsonb('Surimi, bâtonnets, tranche ou râpé saveur crabe'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'epaule d agneau crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Épaule d’agneau crue', 'epaule d agneau crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Épaule d’agneau crue', 'epaule d agneau crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '21504:epaule d agneau crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 170, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 18.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.45, 'g', 'estimated'),
      (v_profile, 'fat_g', 10.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Épaule d’agneau crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '21504', to_jsonb('Agneau, épaule, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'epaule de porc crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Épaule de porc crue', 'epaule de porc crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Épaule de porc crue', 'epaule de porc crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28001:epaule de porc crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 178, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 18.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.38, 'g', 'estimated'),
      (v_profile, 'fat_g', 11.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Épaule de porc crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28001', to_jsonb('Porc, épaule, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'epaule de veau crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Épaule de veau crue', 'epaule de veau crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Épaule de veau crue', 'epaule de veau crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6560:epaule de veau crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 124, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.63, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Épaule de veau crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6560', to_jsonb('Veau, épaule, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'epaule de veau crue en cubes' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Épaule de veau crue', 'epaule de veau crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Épaule de veau crue, en cubes', 'epaule de veau crue en cubes',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6560:epaule de veau crue en cubes', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 124, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.63, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Épaule de veau crue, en cubes', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6560', to_jsonb('Veau, épaule, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'epinard frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Épinard frais', 'epinard frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Épinard frais', 'epinard frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20059:epinard frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 24, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.62, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.25, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.37, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Épinard frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20059', to_jsonb('Épinard, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'volailles';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'escalope de dinde crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Escalope de dinde crue', 'escalope de dinde crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Escalope de dinde crue', 'escalope de dinde crue',
       NULL, 'raw', NULL,
       'boneless', NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36304:escalope de dinde crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 109, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 24.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.51, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.22, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Escalope de dinde crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36304', to_jsonb('Dinde, escalope, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'escalope de veau crue fine' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Escalope de veau crue fine', 'escalope de veau crue fine', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Escalope de veau crue fine', 'escalope de veau crue fine',
       NULL, 'raw', NULL,
       'boneless', NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6521:escalope de veau crue fine', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 108, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.58, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Escalope de veau crue fine', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6521', to_jsonb('Veau, escalope, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'estragon frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Estragon frais', 'estragon frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Estragon frais', 'estragon frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11092:estragon frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 44, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 6.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Estragon frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11092', to_jsonb('Estragon, frais'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'farine de ble t55' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Farine de blé T55', 'farine de ble t55', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Farine de blé T55', 'farine de ble t55',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9436:farine de ble t55', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 346, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 73.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Farine de blé T55', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9436', to_jsonb('Farine de blé tendre ou froment T55 (pour pains)'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'farine de ble type 00' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Farine de blé type 00', 'farine de ble type 00', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Farine de blé type 00', 'farine de ble type 00',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9440:farine de ble type 00', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 352, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.94, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 75.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.82, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Farine de blé type 00', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9440', to_jsonb('Farine de blé tendre ou froment T45 (pour pâtisserie)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'farine de mais blanche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Farine de maïs blanche', 'farine de mais blanche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Farine de maïs blanche', 'farine de mais blanche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9545:farine de mais blanche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 361, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.23, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.55, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Farine de maïs blanche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9545', to_jsonb('Farine de maïs'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'farine de pois chiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Farine de pois chiche', 'farine de pois chiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Farine de pois chiche', 'farine de pois chiche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9580:farine de pois chiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 359, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 47, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.69, 'g', 'estimated'),
      (v_profile, 'fiber_g', 10.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Farine de pois chiche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9580', to_jsonb('Farine de pois chiche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'farine de pois chiche grillee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Farine de pois chiche grillée', 'farine de pois chiche grillee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Farine de pois chiche grillée', 'farine de pois chiche grillee',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9580:farine de pois chiche grillee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 359, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 47, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.69, 'g', 'estimated'),
      (v_profile, 'fiber_g', 10.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Farine de pois chiche grillée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9580', to_jsonb('Farine de pois chiche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'fecule de mais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Fécule de maïs', 'fecule de mais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Fécule de maïs', 'fecule de mais',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9510:fecule de mais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 362, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.43, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 89.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.25, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Fécule de maïs', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9510', to_jsonb('Amidon de maïs ou fécule de maïs'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'fenouil frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Fenouil frais', 'fenouil frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Fenouil frais', 'fenouil frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20028:fenouil frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 21.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Fenouil frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20028', to_jsonb('Fenouil, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'fenugrec en graines' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Fenugrec en graines', 'fenugrec en graines', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Fenugrec en graines', 'fenugrec en graines',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11077:fenugrec en graines', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 27.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 33.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.41, 'g', 'estimated'),
      (v_profile, 'fiber_g', 24.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Fenugrec en graines', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11077', to_jsonb('Fenugrec, graine'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'fenugrec seche kasuri methi' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Fenugrec séché kasuri methi', 'fenugrec seche kasuri methi', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Fenugrec séché kasuri methi', 'fenugrec seche kasuri methi',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11077:fenugrec seche kasuri methi', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 27.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 33.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.41, 'g', 'estimated'),
      (v_profile, 'fiber_g', 24.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Fenugrec séché kasuri methi', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11077', to_jsonb('Fenugrec, graine'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'feuille de brick' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Feuille de brick', 'feuille de brick', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Feuille de brick', 'feuille de brick',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '51550:feuille de brick', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 283, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 59, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Feuille de brick', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '51550', to_jsonb('Khatfa feuille de brick, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'feuille de combava' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Feuille de combava', 'feuille de combava', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Feuille de combava', 'feuille de combava',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11053:feuille de combava', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 353, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.61, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 48.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 8.36, 'g', 'estimated'),
      (v_profile, 'fiber_g', 26.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 0.2, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Feuille de combava', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11053', to_jsonb('Laurier, feuille'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'feuille de curry fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Feuille de curry fraîche', 'feuille de curry fraiche', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Feuille de curry fraîche', 'feuille de curry fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'u', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11070:feuille de curry fraiche', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 107, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 5.56, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 10.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.68, 'g', 'estimated'),
      (v_profile, 'fiber_g', 14, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 0.2, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Feuille de curry fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11070', to_jsonb('Thym, frais'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'feuille de lasagne seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Feuille de lasagne sèche', 'feuille de lasagne seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Feuille de lasagne sèche', 'feuille de lasagne seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9810:feuille de lasagne seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 331, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 65.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.79, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Feuille de lasagne sèche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9810', to_jsonb('Pâtes sèches standard, crues'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'feuille de laurier sechee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Feuille de laurier séchée', 'feuille de laurier sechee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Feuille de laurier séchée', 'feuille de laurier sechee',
       NULL, NULL, 'dried',
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11053:feuille de laurier sechee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 353, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.61, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 48.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 8.36, 'g', 'estimated'),
      (v_profile, 'fiber_g', 26.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 0.2, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Feuille de laurier séchée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11053', to_jsonb('Laurier, feuille'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'feuille de sauge fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Feuille de sauge fraîche', 'feuille de sauge fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Feuille de sauge fraîche', 'feuille de sauge fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11069:feuille de sauge fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 36.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.97, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 0.5, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Feuille de sauge fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11069', to_jsonb('Sauge, fraîche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'feuille de vigne en saumure' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Feuille de vigne en saumure', 'feuille de vigne en saumure', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Feuille de vigne en saumure', 'feuille de vigne en saumure',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '169393:feuille de vigne en saumure', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 69, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4.27, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.97, 'g', 'estimated'),
      (v_profile, 'fiber_g', 9.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 4, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Feuille de vigne en saumure', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '169393', to_jsonb('Grape leaves, canned'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'filet de boeuf cru en lanieres' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Filet de bœuf cru en lanières', 'filet de boeuf cru en lanieres', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Filet de bœuf cru en lanières', 'filet de boeuf cru en lanieres',
       NULL, 'raw', NULL,
       'boneless', NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6116:filet de boeuf cru en lanieres', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 132, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 21.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.22, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.95, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Filet de bœuf cru en lanières', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6116', to_jsonb('Boeuf, filet, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'filet mignon de porc cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Filet mignon de porc cru', 'filet mignon de porc cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Filet mignon de porc cru', 'filet mignon de porc cru',
       NULL, 'raw', NULL,
       'boneless', NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28204:filet mignon de porc cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 123, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 21.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.09, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Filet mignon de porc cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28204', to_jsonb('Porc, filet mignon, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'flanchet de boeuf cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Flanchet de bœuf cru', 'flanchet de boeuf cru', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Flanchet de bœuf cru', 'flanchet de boeuf cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6122:flanchet de boeuf cru', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 287, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 18.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 23.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Flanchet de bœuf cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6122', to_jsonb('Boeuf, plat de côtes, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'frisee fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Frisée fraîche', 'frisee fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Frisée fraîche', 'frisee fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20012:frisee fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 17.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.48, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.25, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.45, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Frisée fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20012', to_jsonb('Salade ou chicorée frisée, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'fromage de chevre buche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Fromage de chèvre bûche', 'fromage de chevre buche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Fromage de chèvre bûche', 'fromage de chevre buche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '12812:fromage de chevre buche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 287, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 18.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.925, 'g', 'estimated'),
      (v_profile, 'fat_g', 23.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Fromage de chèvre bûche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '12812', to_jsonb('Fromage de chèvre bûche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'fromage frais mexicain' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Fromage frais mexicain', 'fromage frais mexicain', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Fromage frais mexicain', 'fromage frais mexicain',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '12060:fromage frais mexicain', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 271, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 16, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.42, 'g', 'estimated'),
      (v_profile, 'fat_g', 21.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Fromage frais mexicain', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '12060', to_jsonb('Fromage type feta, au lait de vache'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'fumet de poisson' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Fumet de poisson', 'fumet de poisson', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Fumet de poisson', 'fumet de poisson',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '25919:fumet de poisson', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 29, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.91, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5.09, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.21, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Fumet de poisson', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '25919', to_jsonb('Soupe de poissons et / ou crustacés, déshydratée reconstituée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'galanga frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Galanga frais', 'galanga frais', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Galanga frais', 'galanga frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11074:galanga frais', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 32.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Galanga frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11074', to_jsonb('Gingembre, racine crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'garam masala' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Garam masala', 'garam masala', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Garam masala', 'garam masala',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11005:garam masala', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 301, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 53.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Garam masala', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11005', to_jsonb('Curry, poudre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gateau de riz coreen tteok' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gâteau de riz coréen tteok', 'gateau de riz coreen tteok', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gâteau de riz coréen tteok', 'gateau de riz coreen tteok',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26265:gateau de riz coreen tteok', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 200, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 37, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gâteau de riz coréen tteok', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26265', to_jsonb('Gnocchi à la semoule, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gelatine feuille' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gélatine feuille', 'gelatine feuille', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gélatine feuille', 'gelatine feuille',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11007:gelatine feuille', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 348, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 86.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gélatine feuille', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11007', to_jsonb('Gélatine, sèche'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ghee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ghee', 'ghee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ghee', 'ghee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '171314:ghee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 100, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ghee', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '171314', to_jsonb('Butter, Clarified butter (ghee)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gigot d agneau cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gigot d''agneau cru', 'gigot d agneau cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gigot d''agneau cru', 'gigot d agneau cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '21502:gigot d agneau cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 128, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.48, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.13, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gigot d''agneau cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '21502', to_jsonb('Agneau, gigot, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gingembre frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gingembre frais', 'gingembre frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gingembre frais', 'gingembre frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11074:gingembre frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 32.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gingembre frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11074', to_jsonb('Gingembre, racine crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gingembre marine rouge' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gingembre mariné rouge', 'gingembre marine rouge', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gingembre mariné rouge', 'gingembre marine rouge',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11074:gingembre marine rouge', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 32.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gingembre mariné rouge', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11074', to_jsonb('Gingembre, racine crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gingembre moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gingembre moulu', 'gingembre moulu', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gingembre moulu', 'gingembre moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11006:gingembre moulu', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 335, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 8.98, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 58.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.24, 'g', 'estimated'),
      (v_profile, 'fiber_g', 14.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gingembre moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11006', to_jsonb('Gingembre, poudre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gnocchi de pomme de terre frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gnocchi de pomme de terre frais', 'gnocchi de pomme de terre frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gnocchi de pomme de terre frais', 'gnocchi de pomme de terre frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26264:gnocchi de pomme de terre frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 176, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4.66, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 35, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.42, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gnocchi de pomme de terre frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26264', to_jsonb('Gnocchi à la pomme de terre, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gochugaru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gochugaru', 'gochugaru', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gochugaru', 'gochugaru',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11088:gochugaru', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 376, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 29.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 27.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gochugaru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11088', to_jsonb('Poivre de Cayenne ou piment de Cayenne'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gochujang' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gochujang', 'gochujang', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gochujang', 'gochujang',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20916:gochujang', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 163, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 11.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 18.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.31, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gochujang', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20916', to_jsonb('Miso'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gousse de vanille' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gousse de vanille', 'gousse de vanille', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gousse de vanille', 'gousse de vanille',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11081:gousse de vanille', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 345, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 37.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 10.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 25.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 4, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gousse de vanille', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11081', to_jsonb('Epice (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'graine de moutarde' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Graine de moutarde', 'graine de moutarde', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Graine de moutarde', 'graine de moutarde',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15034:graine de moutarde', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 436.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 23.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 6.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 36.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 27.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Graine de moutarde', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15034', to_jsonb('Lin, graine'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'gruyere rape' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Gruyère râpé', 'gruyere rape', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Gruyère râpé', 'gruyere rape',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '12114:gruyere rape', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 426, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 27.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.75, 'g', 'estimated'),
      (v_profile, 'fat_g', 34.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Gruyère râpé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '12114', to_jsonb('Gruyère'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'guanciale cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Guanciale cru', 'guanciale cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Guanciale cru', 'guanciale cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28858:guanciale cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 342, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.75, 'g', 'estimated'),
      (v_profile, 'fat_g', 28.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Guanciale cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28858', to_jsonb('Pancetta ou Poitrine roulée sèche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot blanc cuit egoutte' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot blanc cuit', 'haricot blanc cuit', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot blanc cuit, égoutté', 'haricot blanc cuit egoutte',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20502:haricot blanc cuit egoutte', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 112, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.75, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 12, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 13.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot blanc cuit, égoutté', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20502', to_jsonb('Haricot blanc, bouilli/cuit à l''eau'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot blanc sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot blanc sec', 'haricot blanc sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot blanc sec', 'haricot blanc sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20501:haricot blanc sec', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 268, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 43.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.78, 'g', 'estimated'),
      (v_profile, 'fiber_g', 16.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot blanc sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20501', to_jsonb('Haricot blanc, sec'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot cornille sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot cornille sec', 'haricot cornille sec', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot cornille sec', 'haricot cornille sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20525:haricot cornille sec', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 283.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 46.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.06, 'g', 'estimated'),
      (v_profile, 'fiber_g', 15.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot cornille sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20525', to_jsonb('Haricot rouge, sec'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot flageolet sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot flageolet sec', 'haricot flageolet sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot flageolet sec', 'haricot flageolet sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20539:haricot flageolet sec', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 268.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 42.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 23.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot flageolet sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20539', to_jsonb('Haricot flageolet, vert, sec'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot geant sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot géant sec', 'haricot geant sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot géant sec', 'haricot geant sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20501:haricot geant sec', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 268, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 43.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.78, 'g', 'estimated'),
      (v_profile, 'fiber_g', 16.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot géant sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20501', to_jsonb('Haricot blanc, sec'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot kilometre cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot kilomètre cru', 'haricot kilometre cru', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot kilomètre cru', 'haricot kilometre cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20061:haricot kilometre cru', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 25.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.85, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.14, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.21, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.85, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot kilomètre cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20061', to_jsonb('Haricot vert, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot lingot sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot lingot sec', 'haricot lingot sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot lingot sec', 'haricot lingot sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20501:haricot lingot sec', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 268, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 43.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.78, 'g', 'estimated'),
      (v_profile, 'fiber_g', 16.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot lingot sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20501', to_jsonb('Haricot blanc, sec'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot noir sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot noir sec', 'haricot noir sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot noir sec', 'haricot noir sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20525:haricot noir sec', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 283.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 46.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.06, 'g', 'estimated'),
      (v_profile, 'fiber_g', 15.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot noir sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20525', to_jsonb('Haricot rouge, sec'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot rouge cuit egoutte' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot rouge cuit', 'haricot rouge cuit', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot rouge cuit, égoutté', 'haricot rouge cuit egoutte',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20503:haricot rouge cuit egoutte', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 116, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.63, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 12.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 11.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot rouge cuit, égoutté', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20503', to_jsonb('Haricot rouge, bouilli/cuit à l''eau'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot rouge sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot rouge sec', 'haricot rouge sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot rouge sec', 'haricot rouge sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20525:haricot rouge sec', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 283.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 46.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.06, 'g', 'estimated'),
      (v_profile, 'fiber_g', 15.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot rouge sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20525', to_jsonb('Haricot rouge, sec'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot vert cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot vert cru', 'haricot vert cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot vert cru', 'haricot vert cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20061:haricot vert cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 25.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.85, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.14, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.21, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.85, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot vert cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20061', to_jsonb('Haricot vert, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haricot vert cuit' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haricot vert cuit', 'haricot vert cuit', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haricot vert cuit', 'haricot vert cuit',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20030:haricot vert cuit', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 29.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.17, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haricot vert cuit', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20030', to_jsonb('Haricot vert, cuit'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'harissa' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Harissa', 'harissa', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Harissa', 'harissa',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11112:harissa', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 66.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.72, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Harissa', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11112', to_jsonb('Harissa (sauce condimentaire), préemballée'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'volailles';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haut de cuisse de poulet cru desosse' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haut de cuisse de poulet cru', 'haut de cuisse de poulet cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haut de cuisse de poulet cru, désossé', 'haut de cuisse de poulet cru desosse',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36019:haut de cuisse de poulet cru desosse', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 116, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.12, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haut de cuisse de poulet cru, désossé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36019', to_jsonb('Poulet, haut de cuisse, viande, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'volailles';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'haut de cuisse de poulet cru desosse sans peau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Haut de cuisse de poulet cru', 'haut de cuisse de poulet cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Haut de cuisse de poulet cru, désossé, sans peau', 'haut de cuisse de poulet cru desosse sans peau',
       NULL, 'raw', NULL,
       NULL, 'skinless',
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36019:haut de cuisse de poulet cru desosse sans peau', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 116, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.12, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Haut de cuisse de poulet cru, désossé, sans peau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36019', to_jsonb('Poulet, haut de cuisse, viande, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'herbes de provence sechees' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Herbes de Provence séchées', 'herbes de provence sechees', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Herbes de Provence séchées', 'herbes de provence sechees',
       NULL, NULL, 'dried',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11060:herbes de provence sechees', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 283, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 11.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 23, 'g', 'estimated'),
      (v_profile, 'fat_g', 7.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 40.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Herbes de Provence séchées', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11060', to_jsonb('Herbes de Provence, séchées'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile d arachide' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile d’arachide', 'huile d arachide', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile d’arachide', 'huile d arachide',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17040:huile d arachide', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile d’arachide', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17040', to_jsonb('Huile d''arachide'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile d olive vierge extra' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile d''olive vierge extra', 'huile d olive vierge extra', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile d''olive vierge extra', 'huile d olive vierge extra',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17270:huile d olive vierge extra', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile d''olive vierge extra', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17270', to_jsonb('Huile d''olive vierge extra'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile de colza raffinee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile de colza raffinée', 'huile de colza raffinee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile de colza raffinée', 'huile de colza raffinee',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17130:huile de colza raffinee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 100, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile de colza raffinée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17130', to_jsonb('Huile de colza'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile de dende' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile de dendê', 'huile de dende', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile de dendê', 'huile de dende',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '16129:huile de dende', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 100, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile de dendê', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '16129', to_jsonb('Huile de palme, sans précision'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile de friture' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile de friture', 'huile de friture', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile de friture', 'huile de friture',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17001:huile de friture', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.21, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile de friture', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17001', to_jsonb('Huile végétale (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile de moutarde' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile de moutarde', 'huile de moutarde', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile de moutarde', 'huile de moutarde',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17130:huile de moutarde', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 100, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile de moutarde', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17130', to_jsonb('Huile de colza'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile de noix' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile de noix', 'huile de noix', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile de noix', 'huile de noix',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17220:huile de noix', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 100, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile de noix', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17220', to_jsonb('Huile de noix'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile de palme rouge' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile de palme rouge', 'huile de palme rouge', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile de palme rouge', 'huile de palme rouge',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '16129:huile de palme rouge', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 100, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile de palme rouge', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '16129', to_jsonb('Huile de palme, sans précision'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile de sesame grille' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile de sésame grillé', 'huile de sesame grille', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile de sésame grillé', 'huile de sesame grille',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17400:huile de sesame grille', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 100, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile de sésame grillé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17400', to_jsonb('Huile de sésame'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile pimentee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile pimentée', 'huile pimentee', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile pimentée', 'huile pimentee',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17001:huile pimentee', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.21, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile pimentée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17001', to_jsonb('Huile végétale (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile pimentee au roucou' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile pimentée au roucou', 'huile pimentee au roucou', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile pimentée au roucou', 'huile pimentee au roucou',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17001:huile pimentee au roucou', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.21, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile pimentée au roucou', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17001', to_jsonb('Huile végétale (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile vegetale' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile végétale', 'huile vegetale', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile végétale', 'huile vegetale',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17001:huile vegetale', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.21, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile végétale', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17001', to_jsonb('Huile végétale (aliment moyen)'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'huile vegetale raffinee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Huile végétale raffinée', 'huile vegetale raffinee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Huile végétale raffinée', 'huile vegetale raffinee',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '17001:huile vegetale raffinee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 900, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.21, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.92, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Huile végétale raffinée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '17001', to_jsonb('Huile végétale (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jaggery' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jaggery', 'jaggery', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jaggery', 'jaggery',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31017:jaggery', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 389.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 97.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jaggery', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31017', to_jsonb('Sucre roux'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jambon blanc cuit' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jambon blanc cuit', 'jambon blanc cuit', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jambon blanc cuit', 'jambon blanc cuit',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28925:jambon blanc cuit', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 115, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.08, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.42, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'tranche', 'g', 40, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jambon blanc cuit', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28925', to_jsonb('Jambon cuit, de Paris, découenné dégraissé'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jambon de bayonne' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jambon de Bayonne', 'jambon de bayonne', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jambon de Bayonne', 'jambon de bayonne',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28811:jambon de bayonne', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 228, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 28, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 6.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jambon de Bayonne', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28811', to_jsonb('Jambon de Bayonne'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jambon serrano' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jambon serrano', 'jambon serrano', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jambon serrano', 'jambon serrano',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28845:jambon serrano', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 235, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 30.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.76, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.02, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jambon serrano', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28845', to_jsonb('Jambon sec Serrano'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jarret de boeuf cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jarret de bœuf cru', 'jarret de boeuf cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jarret de bœuf cru', 'jarret de boeuf cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6150:jarret de boeuf cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 122, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.25, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jarret de bœuf cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6150', to_jsonb('Boeuf, jarret, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jarret de boeuf cru avec os' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jarret de bœuf cru', 'jarret de boeuf cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jarret de bœuf cru, avec os', 'jarret de boeuf cru avec os',
       NULL, 'raw', NULL,
       'bone_in', NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6150:jarret de boeuf cru avec os', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 122, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.25, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jarret de bœuf cru, avec os', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6150', to_jsonb('Boeuf, jarret, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jarret de veau cru en tranches avec os' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jarret de veau cru', 'jarret de veau cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jarret de veau cru, en tranches avec os', 'jarret de veau cru en tranches avec os',
       NULL, 'raw', NULL,
       'bone_in', NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6583:jarret de veau cru en tranches avec os', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 118, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 21.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.66, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jarret de veau cru, en tranches avec os', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6583', to_jsonb('Veau, jarret, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'oeufs';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jaune d oeuf cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jaune d''œuf cru', 'jaune d oeuf cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jaune d''œuf cru', 'jaune d oeuf cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '22002:jaune d oeuf cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 307, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 15.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.09, 'g', 'estimated'),
      (v_profile, 'fat_g', 26.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 18, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jaune d''œuf cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '22002', to_jsonb('Oeuf, jaune (jaune d''oeuf), cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'oeufs';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jaune d oeuf cuit' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jaune d’œuf cuit', 'jaune d oeuf cuit', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jaune d’œuf cuit', 'jaune d oeuf cuit',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '22009:jaune d oeuf cuit', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 340, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 16, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.31, 'g', 'estimated'),
      (v_profile, 'fat_g', 30.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 18, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jaune d’œuf cuit', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '22009', to_jsonb('Oeuf, jaune (jaune d''oeuf), cuit'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jus d orange amere' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jus d orange amère', 'jus d orange amere', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jus d orange amère', 'jus d orange amere',
       'liquid', NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '2070:jus d orange amere', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.61, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 9.61, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.11, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.28, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.03, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jus d orange amère', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '2070', to_jsonb('Jus d''orange, pur jus'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jus de citron frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jus de citron frais', 'jus de citron frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jus de citron frais', 'jus de citron frais',
       'liquid', NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '2007:jus de citron frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 13.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.49, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.41, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.24, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.03, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jus de citron frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '2007', to_jsonb('Jus de citron, maison'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'jus de citron vert frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Jus de citron vert frais', 'jus de citron vert frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Jus de citron vert frais', 'jus de citron vert frais',
       'liquid', NULL, 'fresh',
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '2030:jus de citron vert frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 9.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.42, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.69, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.07, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.03, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Jus de citron vert frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '2030', to_jsonb('Jus de citron vert, maison'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'kecap manis' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Kecap manis', 'kecap manis', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Kecap manis', 'kecap manis',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31067:kecap manis', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 299.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 74.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.3, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Kecap manis', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31067', to_jsonb('Mélasse de canne'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'kimchi de chou fermente mur' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Kimchi de chou fermenté mûr', 'kimchi de chou fermente mur', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Kimchi de chou fermenté mûr', 'kimchi de chou fermente mur',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20116:kimchi de chou fermente mur', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 36.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.38, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Kimchi de chou fermenté mûr', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20116', to_jsonb('Chou blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lait de coco' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lait de coco', 'lait de coco', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lait de coco', 'lait de coco',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '18041:lait de coco', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 188, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.77, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 18.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.57, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.03, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lait de coco', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '18041', to_jsonb('Lait de coco ou Crème de coco'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lait demi ecreme' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lait demi-écrémé', 'lait demi ecreme', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lait demi-écrémé', 'lait demi ecreme',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19041:lait demi ecreme', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 47.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.31, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.83, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.55, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.03, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lait demi-écrémé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19041', to_jsonb('Lait demi-écrémé, UHT'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lait entier' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lait entier', 'lait entier', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lait entier', 'lait entier',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19023:lait entier', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 65.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.25, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.85, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.63, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.03, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lait entier', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19023', to_jsonb('Lait entier, UHT'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lait evapore' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lait évaporé', 'lait evapore', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lait évaporé', 'lait evapore',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19026:lait evapore', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 123, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.31, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 9.91, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.06, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lait évaporé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19026', to_jsonb('Lait concentré non sucré, entier'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'laitue fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Laitue fraîche', 'laitue fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Laitue fraîche', 'laitue fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20031:laitue fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 12.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.33, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Laitue fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20031', to_jsonb('Laitue, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'langue de boeuf crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Langue de bœuf crue', 'langue de boeuf crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Langue de bœuf crue', 'langue de boeuf crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '40202:langue de boeuf crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 193, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 16.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 13.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Langue de bœuf crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '40202', to_jsonb('Langue, boeuf, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lapin cru decoupe' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lapin cru', 'lapin cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lapin cru, découpé', 'lapin cru decoupe',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '34001:lapin cru decoupe', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 189, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.66, 'g', 'estimated'),
      (v_profile, 'fat_g', 11.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lapin cru, découpé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '34001', to_jsonb('Lapin, viande crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lardon fume cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lardon fumé cru', 'lardon fume cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lardon fumé cru', 'lardon fume cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28720:lardon fume cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 275, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 16.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.96, 'g', 'estimated'),
      (v_profile, 'fat_g', 22.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.34, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lardon fumé cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28720', to_jsonb('Lardon fumé, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lentille brune seche crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lentille brune sèche', 'lentille brune seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lentille brune sèche, crue', 'lentille brune seche crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20504:lentille brune seche crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 316.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 25.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 50.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.34, 'g', 'estimated'),
      (v_profile, 'fiber_g', 7.63, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lentille brune sèche, crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20504', to_jsonb('Lentille, sèche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lentille corail seche crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lentille corail sèche', 'lentille corail seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lentille corail sèche, crue', 'lentille corail seche crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20535:lentille corail seche crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 297.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 27.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 44.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 15.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lentille corail sèche, crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20535', to_jsonb('Lentille corail, sèche'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lentille urad blanche seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lentille urad blanche sèche', 'lentille urad blanche seche', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lentille urad blanche sèche', 'lentille urad blanche seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20530:lentille urad blanche seche', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 301.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 24.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 47.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.42, 'g', 'estimated'),
      (v_profile, 'fiber_g', 16.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lentille urad blanche sèche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20530', to_jsonb('Haricot mungo, sec'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lentille urad noire entiere seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lentille urad noire entière sèche', 'lentille urad noire entiere seche', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lentille urad noire entière sèche', 'lentille urad noire entiere seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20504:lentille urad noire entiere seche', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 316.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 25.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 50.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.34, 'g', 'estimated'),
      (v_profile, 'fiber_g', 7.63, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lentille urad noire entière sèche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20504', to_jsonb('Lentille, sèche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'lentille verte seche crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Lentille verte sèche', 'lentille verte seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Lentille verte sèche, crue', 'lentille verte seche crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20585:lentille verte seche crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 294.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 25.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 44.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 16.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Lentille verte sèche, crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20585', to_jsonb('Lentille verte, sèche'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'levure boulangere fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Levure boulangère fraîche', 'levure boulangere fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Levure boulangère fraîche', 'levure boulangere fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11010:levure boulangere fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 97.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 8.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 7.15, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Levure boulangère fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11010', to_jsonb('Levure de boulanger, compressée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'levure boulangere seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Levure boulangère sèche', 'levure boulangere seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Levure boulangère sèche', 'levure boulangere seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11045:levure boulangere seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 306.9, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 44.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 17.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.37, 'g', 'estimated'),
      (v_profile, 'fiber_g', 23.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Levure boulangère sèche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11045', to_jsonb('Levure de boulanger, déshydratée'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'levure chimique' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Levure chimique', 'levure chimique', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Levure chimique', 'levure chimique',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11046:levure chimique', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 142.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.96, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 33.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Levure chimique', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11046', to_jsonb('Levure chimique ou Poudre à lever'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'linguica portugaise' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Linguiça portugaise', 'linguica portugaise', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Linguiça portugaise', 'linguica portugaise',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '173859:linguica portugaise', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 296, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.78, 'g', 'estimated'),
      (v_profile, 'fat_g', 25.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 120, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Linguiça portugaise', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '173859', to_jsonb('Sausage, pork, chorizo, link or ground, raw'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'marsala' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Marsala', 'marsala', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Marsala', 'marsala',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '1015:marsala', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 211, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 27.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Marsala', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '1015', to_jsonb('Marsala'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'masa harina' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Masa harina', 'masa harina', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Masa harina', 'masa harina',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '169696:masa harina', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 363, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 8.46, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 76.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.69, 'g', 'estimated'),
      (v_profile, 'fiber_g', 6.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Masa harina', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '169696', to_jsonb('Corn flour, masa, unenriched, white'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'mascarpone' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Mascarpone', 'mascarpone', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Mascarpone', 'mascarpone',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19584:mascarpone', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 384.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4.29, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4, 'g', 'estimated'),
      (v_profile, 'fat_g', 39, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Mascarpone', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19584', to_jsonb('Mascarpone'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'mayonnaise' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Mayonnaise', 'mayonnaise', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Mayonnaise', 'mayonnaise',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11054:mayonnaise', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 692.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.36, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 75.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.32, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Mayonnaise', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11054', to_jsonb('Mayonnaise (70% MG min.), préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'mayonnaise japonaise' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Mayonnaise japonaise', 'mayonnaise japonaise', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Mayonnaise japonaise', 'mayonnaise japonaise',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11054:mayonnaise japonaise', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 692.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.36, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 75.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.32, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Mayonnaise japonaise', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11054', to_jsonb('Mayonnaise (70% MG min.), préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'melasse de grenade' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Mélasse de grenade', 'melasse de grenade', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Mélasse de grenade', 'melasse de grenade',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31067:melasse de grenade', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 299.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 74.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.33, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Mélasse de grenade', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31067', to_jsonb('Mélasse de canne'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'menthe fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Menthe fraîche', 'menthe fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Menthe fraîche', 'menthe fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11027:menthe fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 57.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.52, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.84, 'g', 'estimated'),
      (v_profile, 'fiber_g', 7.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Menthe fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11027', to_jsonb('Menthe, fraîche'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'menthe sechee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Menthe séchée', 'menthe sechee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Menthe séchée', 'menthe sechee',
       NULL, NULL, 'dried',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11029:menthe sechee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 283, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 22.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.03, 'g', 'estimated'),
      (v_profile, 'fiber_g', 29.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Menthe séchée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11029', to_jsonb('Menthe, séchée'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'merguez crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Merguez crue', 'merguez crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Merguez crue', 'merguez crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '30150:merguez crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 316, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 28, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Merguez crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '30150', to_jsonb('Merguez, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'miel' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Miel', 'miel', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Miel', 'miel',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31008:miel', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 329, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.56, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 81.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Miel', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31008', to_jsonb('Miel'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'mirin' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Mirin', 'mirin', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Mirin', 'mirin',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '1015:mirin', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 211, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 27.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.15, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Mirin', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '1015', to_jsonb('Marsala'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'morcilla asturienne' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Morcilla asturienne', 'morcilla asturienne', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Morcilla asturienne', 'morcilla asturienne',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '8703:morcilla asturienne', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 313, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 27.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.42, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Morcilla asturienne', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '8703', to_jsonb('Boudin noir, à cuire'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'morue salee seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Morue salée sèche', 'morue salee seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Morue salée sèche', 'morue salee seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26098:morue salee seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 206, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 47.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.67, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Morue salée sèche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26098', to_jsonb('Morue, salée, sèche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'moule commune crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Moule commune crue', 'moule commune crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Moule commune crue', 'moule commune crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '10014:moule commune crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 71.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 11.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.69, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.82, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Moule commune crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '10014', to_jsonb('Moule commune, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'moutarde de dijon' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Moutarde de Dijon', 'moutarde de dijon', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Moutarde de Dijon', 'moutarde de dijon',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11013:moutarde de dijon', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 152, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.92, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.33, 'g', 'estimated'),
      (v_profile, 'fat_g', 11.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Moutarde de Dijon', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11013', to_jsonb('Moutarde'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'mozzarella' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Mozzarella', 'mozzarella', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Mozzarella', 'mozzarella',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19590:mozzarella', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 229, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 16.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.75, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Mozzarella', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19590', to_jsonb('Mozzarella au lait de vache'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'muscade moulue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Muscade moulue', 'muscade moulue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Muscade moulue', 'muscade moulue',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11048:muscade moulue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 504, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.26, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 28.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 36.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 20.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Muscade moulue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11048', to_jsonb('Noix de muscade'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'navet cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Navet cru', 'navet cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Navet cru', 'navet cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20064:navet cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.76, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Navet cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20064', to_jsonb('Navet, pelé, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'noix' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Noix', 'noix', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Noix', 'noix',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15005:noix', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 686.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 15.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 6.88, 'g', 'estimated'),
      (v_profile, 'fat_g', 67.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 6.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Noix', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15005', to_jsonb('Noix, séchée, cerneaux'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'noix de saint jacques crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Noix de Saint-Jacques crue', 'noix de saint jacques crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Noix de Saint-Jacques crue', 'noix de saint jacques crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '10045:noix de saint jacques crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 83.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 17.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.15, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.84, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Noix de Saint-Jacques crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '10045', to_jsonb('Coquille Saint-Jacques, noix, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'nouille de ble chinoise seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Nouille de blé chinoise sèche', 'nouille de ble chinoise seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Nouille de blé chinoise sèche', 'nouille de ble chinoise seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9863:nouille de ble chinoise seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 366, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 71, 'g', 'estimated'),
      (v_profile, 'fat_g', 3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Nouille de blé chinoise sèche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9863', to_jsonb('Pâtes ou nouilles asiatiques au blé et aux oeufs, crues, nature'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'nouille de riz plate seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Nouille de riz plate sèche', 'nouille de riz plate seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Nouille de riz plate sèche', 'nouille de riz plate seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9900:nouille de riz plate seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 363, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.81, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 80.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Nouille de riz plate sèche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9900', to_jsonb('Vermicelle de riz, sèche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'nouille ramen fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Nouille ramen fraîche', 'nouille ramen fraiche', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Nouille ramen fraîche', 'nouille ramen fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9815:nouille ramen fraiche', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 279, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 11.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 53.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.87, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.17, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Nouille ramen fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9815', to_jsonb('Pâtes fraîches, aux oeufs, crues'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'oeufs';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'oeuf cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Œuf cru', 'oeuf cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Œuf cru', 'oeuf cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '22000:oeuf cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 140, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.27, 'g', 'estimated'),
      (v_profile, 'fat_g', 9.83, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 50, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Œuf cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '22000', to_jsonb('Oeuf, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'oeufs';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'oeuf dur' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Œuf dur', 'oeuf dur', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Œuf dur', 'oeuf dur',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '22010:oeuf dur', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 134, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.52, 'g', 'estimated'),
      (v_profile, 'fat_g', 8.62, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 50, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Œuf dur', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '22010', to_jsonb('Oeuf, dur'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'oeufs';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'oeuf marine ajitama' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Œuf mariné ajitama', 'oeuf marine ajitama', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Œuf mariné ajitama', 'oeuf marine ajitama',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '22010:oeuf marine ajitama', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 134, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.52, 'g', 'estimated'),
      (v_profile, 'fat_g', 8.62, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 50, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Œuf mariné ajitama', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '22010', to_jsonb('Oeuf, dur'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'oignon blanc cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Oignon blanc cru', 'oignon blanc cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Oignon blanc cru', 'oignon blanc cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20034:oignon blanc cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 35, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 6.25, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.62, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Oignon blanc cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20034', to_jsonb('Oignon, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'oignon en poudre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Oignon en poudre', 'oignon en poudre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Oignon en poudre', 'oignon en poudre',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '171327:oignon en poudre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 341, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 10.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 79.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.04, 'g', 'estimated'),
      (v_profile, 'fiber_g', 15.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Oignon en poudre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '171327', to_jsonb('Spices, onion powder'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'oignon grelot cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Oignon grelot cru', 'oignon grelot cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Oignon grelot cru', 'oignon grelot cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20034:oignon grelot cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 35, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 6.25, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.62, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Oignon grelot cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20034', to_jsonb('Oignon, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'oignon jaune cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Oignon jaune cru', 'oignon jaune cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Oignon jaune cru', 'oignon jaune cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20239:oignon jaune cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 37.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.19, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 6.39, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Oignon jaune cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20239', to_jsonb('Oignon jaune, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'oignon nouveau frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Oignon nouveau frais', 'oignon nouveau frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Oignon nouveau frais', 'oignon nouveau frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20323:oignon nouveau frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 27.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.13, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.56, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Oignon nouveau frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20323', to_jsonb('Oignon nouveau ou oignon frais ou cébette, sauté/poêlé sans matière grasse'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'oignon rouge cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Oignon rouge cru', 'oignon rouge cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Oignon rouge cru', 'oignon rouge cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20238:oignon rouge cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 36.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.31, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Oignon rouge cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20238', to_jsonb('Oignon rouge, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'olive noire' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Olive noire', 'olive noire', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Olive noire', 'olive noire',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13032:olive noire', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 160.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.38, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 6.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Olive noire', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13032', to_jsonb('Olive noire, en saumure, égouttée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'olive noire denoyautee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Olive noire dénoyautée', 'olive noire denoyautee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Olive noire dénoyautée', 'olive noire denoyautee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13032:olive noire denoyautee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 160.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.38, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 6.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Olive noire dénoyautée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13032', to_jsonb('Olive noire, en saumure, égouttée'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'olive verte denoyautee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Olive verte dénoyautée', 'olive verte denoyautee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Olive verte dénoyautée', 'olive verte denoyautee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13033:olive verte denoyautee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 155, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.31, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.315, 'g', 'estimated'),
      (v_profile, 'fat_g', 15.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Olive verte dénoyautée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13033', to_jsonb('Olive verte, en saumure, égouttée'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'orange fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Orange fraîche', 'orange fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Orange fraîche', 'orange fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13034:orange fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.75, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 8.03, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Orange fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13034', to_jsonb('Orange, pulpe, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'origan mexicain seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Origan mexicain séché', 'origan mexicain seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Origan mexicain séché', 'origan mexicain seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11035:origan mexicain seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 265, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 26.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.28, 'g', 'estimated'),
      (v_profile, 'fiber_g', 42.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Origan mexicain séché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11035', to_jsonb('Origan, séché'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'origan seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Origan séché', 'origan seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Origan séché', 'origan seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11035:origan seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 265, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 26.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.28, 'g', 'estimated'),
      (v_profile, 'fiber_g', 42.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Origan séché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11035', to_jsonb('Origan, séché'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'os a moelle de boeuf' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Os à moelle de bœuf', 'os a moelle de boeuf', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Os à moelle de bœuf', 'os a moelle de boeuf',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6101:os a moelle de boeuf', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 240, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 32.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 50, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Os à moelle de bœuf', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6101', to_jsonb('Boeuf, braisé'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'oseille crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Oseille crue', 'oseille crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Oseille crue', 'oseille crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20111:oseille crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 19.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.84, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.65, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Oseille crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20111', to_jsonb('Oseille, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pain blanc rassis' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pain blanc rassis', 'pain blanc rassis', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pain blanc rassis', 'pain blanc rassis',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7012:pain blanc rassis', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 263, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 8.67, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 52, 'g', 'estimated'),
      (v_profile, 'fat_g', 2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pain blanc rassis', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7012', to_jsonb('Pain courant, 400g ou boule'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pain d epices' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pain d''épices', 'pain d epices', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pain d''épices', 'pain d epices',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '23200:pain d epices', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 307.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.94, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 71.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pain d''épices', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '23200', to_jsonb('Pain d''épices'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pain de campagne' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pain de campagne', 'pain de campagne', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pain de campagne', 'pain de campagne',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7100:pain de campagne', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 250, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 8.25, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 50, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pain de campagne', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7100', to_jsonb('Pain, baguette ou boule, de campagne'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pain de mie' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pain de mie', 'pain de mie', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pain de mie', 'pain de mie',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7200:pain de mie', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 275, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.81, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 52.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'tranche', 'g', 30, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pain de mie', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7200', to_jsonb('Pain de mie, courant'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pain pav' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pain pav', 'pain pav', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pain pav', 'pain pav',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7259:pain pav', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 290, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.95, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 49.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.35, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pain pav', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7259', to_jsonb('Pain pour hamburger ou hot dog (bun), préemballé'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pain pita' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pain pita', 'pain pita', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pain pita', 'pain pita',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7180:pain pita', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 256, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 8.26, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 53, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.92, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.68, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pain pita', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7180', to_jsonb('Pain pita'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pain rassis' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pain rassis', 'pain rassis', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pain rassis', 'pain rassis',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7000:pain rassis', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 273, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.01, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 54.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.61, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.84, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pain rassis', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7000', to_jsonb('Pain (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pain toscan rassis' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pain toscan rassis', 'pain toscan rassis', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pain toscan rassis', 'pain toscan rassis',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7160:pain toscan rassis', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 276, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 56.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.68, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pain toscan rassis', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7160', to_jsonb('Pain, baguette, sans sel'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'paleron de boeuf cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Paleron de bœuf cru', 'paleron de boeuf cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Paleron de bœuf cru', 'paleron de boeuf cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6270:paleron de boeuf cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 144, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 21.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.54, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Paleron de bœuf cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6270', to_jsonb('Boeuf, paleron, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'paleron de boeuf cru pare' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Paleron de bœuf cru', 'paleron de boeuf cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Paleron de bœuf cru, paré', 'paleron de boeuf cru pare',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '6270:paleron de boeuf cru pare', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 144, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 21.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.54, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Paleron de bœuf cru, paré', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '6270', to_jsonb('Boeuf, paleron, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'paneer' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Paneer', 'paneer', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Paneer', 'paneer',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '172223:paneer', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 299, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 18.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.98, 'g', 'estimated'),
      (v_profile, 'fat_g', 23.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Paneer', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '172223', to_jsonb('Cheese, fresh, queso fresco'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'panko' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Panko', 'panko', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Panko', 'panko',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7500:panko', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 369, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 70, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.77, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Panko', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7500', to_jsonb('Chapelure'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'papaye verte' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Papaye verte', 'papaye verte', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Papaye verte', 'papaye verte',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13035:papaye verte', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 42.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.75, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 8.53, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Papaye verte', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13035', to_jsonb('Papaye, pulpe, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'paprika doux' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Paprika doux', 'paprika doux', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Paprika doux', 'paprika doux',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11049:paprika doux', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 319, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 19.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 34.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Paprika doux', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11049', to_jsonb('Paprika'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'paprika doux hongrois' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Paprika doux hongrois', 'paprika doux hongrois', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Paprika doux hongrois', 'paprika doux hongrois',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11049:paprika doux hongrois', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 319, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 19.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 34.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Paprika doux hongrois', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11049', to_jsonb('Paprika'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'paprika fort hongrois' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Paprika fort hongrois', 'paprika fort hongrois', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Paprika fort hongrois', 'paprika fort hongrois',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11049:paprika fort hongrois', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 319, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 19.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 34.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Paprika fort hongrois', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11049', to_jsonb('Paprika'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'paprika fume' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Paprika fumé', 'paprika fume', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Paprika fumé', 'paprika fume',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11049:paprika fume', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 319, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 19.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 34.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Paprika fumé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11049', to_jsonb('Paprika'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'parmesan affine' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Parmesan affiné', 'parmesan affine', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Parmesan affiné', 'parmesan affine',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '12120:parmesan affine', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 409, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 30.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.22, 'g', 'estimated'),
      (v_profile, 'fat_g', 31, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Parmesan affiné', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '12120', to_jsonb('Parmesan'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'patate douce crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Patate douce crue', 'patate douce crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Patate douce crue', 'patate douce crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '4101:patate douce crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 86.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.51, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 18.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.15, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.87, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Patate douce crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '4101', to_jsonb('Patate douce, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'patate douce crue epluchee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Patate douce crue', 'patate douce crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Patate douce crue, épluchée', 'patate douce crue epluchee',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '4101:patate douce crue epluchee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 86.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.51, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 18.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.15, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.87, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Patate douce crue, épluchée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '4101', to_jsonb('Patate douce, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pate brisee crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pâte brisée crue', 'pate brisee crue', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pâte brisée crue', 'pate brisee crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '23481:pate brisee crue', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 313, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4.44, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 38.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 15, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pâte brisée crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '23481', to_jsonb('Tarte normande aux pommes (garniture farine, oeufs, crème, sucre, calvados)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pate d arachide pure' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pâte d’arachide pure', 'pate d arachide pure', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pâte d’arachide pure', 'pate d arachide pure',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15202:pate d arachide pure', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 625.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 25.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 16.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 52.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pâte d’arachide pure', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15202', to_jsonb('Beurre de cacahuète ou Pâte d''arachide'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pate feuilletee crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pâte feuilletée crue', 'pate feuilletee crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pâte feuilletée crue', 'pate feuilletee crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '23424:pate feuilletee crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 366, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.14, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 39.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 20.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.32, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pâte feuilletée crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '23424', to_jsonb('Pâte feuilletée pur beurre, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pate sablee crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pâte sablée crue', 'pate sablee crue', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pâte sablée crue', 'pate sablee crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '23481:pate sablee crue', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 313, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4.44, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 38.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 15, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pâte sablée crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '23481', to_jsonb('Tarte normande aux pommes (garniture farine, oeufs, crème, sucre, calvados)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pates seches courtes' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pâtes sèches courtes', 'pates seches courtes', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pâtes sèches courtes', 'pates seches courtes',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9810:pates seches courtes', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 331, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 65.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.79, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pâtes sèches courtes', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9810', to_jsonb('Pâtes sèches standard, crues'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pave de saumon cru sans peau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pavé de saumon cru', 'pave de saumon cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pavé de saumon cru, sans peau', 'pave de saumon cru sans peau',
       NULL, 'raw', NULL,
       NULL, 'skinless',
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26036:pave de saumon cru sans peau', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 194, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.25, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pavé de saumon cru, sans peau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26036', to_jsonb('Saumon, cru, élevage'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pecorino romano' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pecorino romano', 'pecorino romano', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pecorino romano', 'pecorino romano',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '12122:pecorino romano', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 387, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 31.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 28.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pecorino romano', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '12122', to_jsonb('Pecorino'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'persil frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Persil frais', 'persil frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Persil frais', 'persil frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11014:persil frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 43, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.71, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.48, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.63, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Persil frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11014', to_jsonb('Persil, frais'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'persil plat frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Persil plat frais', 'persil plat frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Persil plat frais', 'persil plat frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11014:persil plat frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 43, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.71, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.48, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.63, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Persil plat frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11014', to_jsonb('Persil, frais'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'petit pois surgele' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Petit pois surgelé', 'petit pois surgele', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Petit pois surgelé', 'petit pois surgele',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20084:petit pois surgele', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 62.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 5.86, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 8.77, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.44, 'g', 'estimated'),
      (v_profile, 'fiber_g', 6.45, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Petit pois surgelé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20084', to_jsonb('Petits pois, surgelés, crus'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'petites pates seches' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Petites pâtes sèches', 'petites pates seches', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Petites pâtes sèches', 'petites pates seches',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9810:petites pates seches', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 331, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 65.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.79, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Petites pâtes sèches', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9810', to_jsonb('Pâtes sèches standard, crues'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pied de porc cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pied de porc cru', 'pied de porc cru', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pied de porc cru', 'pied de porc cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28540:pied de porc cru', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 137, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 11.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.01, 'g', 'estimated'),
      (v_profile, 'fat_g', 10, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pied de porc cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28540', to_jsonb('Pied de porc demi-sel'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pignon de pin' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pignon de pin', 'pignon de pin', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pignon de pin', 'pignon de pin',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15025:pignon de pin', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 665, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 16.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 6.31, 'g', 'estimated'),
      (v_profile, 'fat_g', 65, 'g', 'estimated'),
      (v_profile, 'fiber_g', 10, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pignon de pin', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15025', to_jsonb('Pignon de pin'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment ancho seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment ancho séché', 'piment ancho seche', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment ancho séché', 'piment ancho seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11049:piment ancho seche', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 319, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 19.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 34.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment ancho séché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11049', to_jsonb('Paprika'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment d espelette moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment d''Espelette moulu', 'piment d espelette moulu', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment d''Espelette moulu', 'piment d espelette moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11088:piment d espelette moulu', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 376, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 29.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 27.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment d''Espelette moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11088', to_jsonb('Poivre de Cayenne ou piment de Cayenne'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment de cayenne' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment de Cayenne', 'piment de cayenne', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment de Cayenne', 'piment de cayenne',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11088:piment de cayenne', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 376, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 29.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 27.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment de Cayenne', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11088', to_jsonb('Poivre de Cayenne ou piment de Cayenne'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment de la jamaique en grains' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment de la Jamaïque en grains', 'piment de la jamaique en grains', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment de la Jamaïque en grains', 'piment de la jamaique en grains',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11056:piment de la jamaique en grains', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 348, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.09, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 50.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 8.69, 'g', 'estimated'),
      (v_profile, 'fiber_g', 21.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 0.1, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment de la Jamaïque en grains', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11056', to_jsonb('Quatre épices'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment de la jamaique moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment de la Jamaïque moulu', 'piment de la jamaique moulu', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment de la Jamaïque moulu', 'piment de la jamaique moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11056:piment de la jamaique moulu', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 348, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.09, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 50.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 8.69, 'g', 'estimated'),
      (v_profile, 'fiber_g', 21.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment de la Jamaïque moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11056', to_jsonb('Quatre épices'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment doux vert frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment doux vert frais', 'piment doux vert frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment doux vert frais', 'piment doux vert frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20085:piment doux vert frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 26, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.81, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.43, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment doux vert frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20085', to_jsonb('Poivron vert, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment du cachemire moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment du Cachemire moulu', 'piment du cachemire moulu', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment du Cachemire moulu', 'piment du cachemire moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11088:piment du cachemire moulu', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 376, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 29.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 27.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment du Cachemire moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11088', to_jsonb('Poivre de Cayenne ou piment de Cayenne'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment en poudre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment en poudre', 'piment en poudre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment en poudre', 'piment en poudre',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11088:piment en poudre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 376, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 29.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 27.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment en poudre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11088', to_jsonb('Poivre de Cayenne ou piment de Cayenne'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment frais', 'piment frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment frais', 'piment frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20151:piment frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 41.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.32, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.65, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 15, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20151', to_jsonb('Piment, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment guajillo seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment guajillo séché', 'piment guajillo seche', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment guajillo séché', 'piment guajillo seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11049:piment guajillo seche', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 319, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 19.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 34.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment guajillo séché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11049', to_jsonb('Paprika'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment habanero' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment habanero', 'piment habanero', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment habanero', 'piment habanero',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20151:piment habanero', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 41.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.32, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.65, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 8, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment habanero', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20151', to_jsonb('Piment, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment jalapeno frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment jalapeño frais', 'piment jalapeno frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment jalapeño frais', 'piment jalapeno frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20151:piment jalapeno frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 41.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.32, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.65, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 15, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment jalapeño frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20151', to_jsonb('Piment, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment oiseau frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment oiseau frais', 'piment oiseau frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment oiseau frais', 'piment oiseau frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20151:piment oiseau frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 41.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.32, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.65, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 1, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment oiseau frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20151', to_jsonb('Piment, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment pasilla seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment pasilla séché', 'piment pasilla seche', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment pasilla séché', 'piment pasilla seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11049:piment pasilla seche', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 319, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 19.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 34.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment pasilla séché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11049', to_jsonb('Paprika'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment rouge frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment rouge frais', 'piment rouge frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment rouge frais', 'piment rouge frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20151:piment rouge frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 41.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.32, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.65, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 15, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment rouge frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20151', to_jsonb('Piment, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment rouge seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment rouge séché', 'piment rouge seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment rouge séché', 'piment rouge seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11088:piment rouge seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 376, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 29.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 27.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment rouge séché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11088', to_jsonb('Poivre de Cayenne ou piment de Cayenne'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment scotch bonnet' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment scotch bonnet', 'piment scotch bonnet', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment scotch bonnet', 'piment scotch bonnet',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20151:piment scotch bonnet', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 41.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.32, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.65, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 10, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment scotch bonnet', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20151', to_jsonb('Piment, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment séché', 'piment seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment séché', 'piment seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11088:piment seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 376, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 29.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 27.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment séché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11088', to_jsonb('Poivre de Cayenne ou piment de Cayenne'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment serrano' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment serrano', 'piment serrano', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment serrano', 'piment serrano',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20151:piment serrano', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 41.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.32, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.65, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 7, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment serrano', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20151', to_jsonb('Piment, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment vert doux frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment vert doux frais', 'piment vert doux frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment vert doux frais', 'piment vert doux frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20085:piment vert doux frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 26, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.81, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.43, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment vert doux frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20085', to_jsonb('Poivron vert, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'piment vert frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Piment vert frais', 'piment vert frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Piment vert frais', 'piment vert frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20151:piment vert frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 41.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.87, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.32, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.65, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 15, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Piment vert frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20151', to_jsonb('Piment, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poire asiatique fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poire asiatique fraîche', 'poire asiatique fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poire asiatique fraîche', 'poire asiatique fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13107:poire asiatique fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 43.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 10.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.47, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poire asiatique fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13107', to_jsonb('Poire, pulpe, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poireau cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poireau cru', 'poireau cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poireau cru', 'poireau cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20039:poireau cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 32.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.49, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.25, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.27, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poireau cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20039', to_jsonb('Poireau, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pois chiche cuit egoutte' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pois chiche cuit', 'pois chiche cuit', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pois chiche cuit, égoutté', 'pois chiche cuit egoutte',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20532:pois chiche cuit egoutte', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 111.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.74, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 15, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.68, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.45, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pois chiche cuit, égoutté', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20532', to_jsonb('Pois chiche, appertisé, égoutté'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pois chiche sec cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pois chiche sec', 'pois chiche sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pois chiche sec, cru', 'pois chiche sec cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20516:pois chiche sec cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 324.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 47.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.85, 'g', 'estimated'),
      (v_profile, 'fiber_g', 13.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pois chiche sec, cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20516', to_jsonb('Pois chiche, sec'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poitrine de porc crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poitrine de porc crue', 'poitrine de porc crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poitrine de porc crue', 'poitrine de porc crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28002:poitrine de porc crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 253, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 17, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 20.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poitrine de porc crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28002', to_jsonb('Porc, poitrine, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poitrine de porc crue avec couenne' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poitrine de porc crue avec couenne', 'poitrine de porc crue avec couenne', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poitrine de porc crue avec couenne', 'poitrine de porc crue avec couenne',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28002:poitrine de porc crue avec couenne', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 253, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 17, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 20.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poitrine de porc crue avec couenne', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28002', to_jsonb('Porc, poitrine, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poitrine de porc crue fine' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poitrine de porc crue fine', 'poitrine de porc crue fine', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poitrine de porc crue fine', 'poitrine de porc crue fine',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28002:poitrine de porc crue fine', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 253, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 17, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 20.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poitrine de porc crue fine', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28002', to_jsonb('Porc, poitrine, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poitrine de porc demi sel' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poitrine de porc demi-sel', 'poitrine de porc demi sel', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poitrine de porc demi-sel', 'poitrine de porc demi sel',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28550:poitrine de porc demi sel', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 268, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 16.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.75, 'g', 'estimated'),
      (v_profile, 'fat_g', 22, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poitrine de porc demi-sel', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28550', to_jsonb('Poitrine de porc demi-sel'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poitrine de porc fumee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poitrine de porc fumée', 'poitrine de porc fumee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poitrine de porc fumée', 'poitrine de porc fumee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28502:poitrine de porc fumee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 303, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 15.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.79, 'g', 'estimated'),
      (v_profile, 'fat_g', 26.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poitrine de porc fumée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28502', to_jsonb('Poitrine de porc, fumée, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poivre blanc moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poivre blanc moulu', 'poivre blanc moulu', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poivre blanc moulu', 'poivre blanc moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11019:poivre blanc moulu', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 310, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 11.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 48.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.11, 'g', 'estimated'),
      (v_profile, 'fiber_g', 26.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poivre blanc moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11019', to_jsonb('Poivre blanc, poudre'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poivre du sichuan' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poivre du Sichuan', 'poivre du sichuan', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poivre du Sichuan', 'poivre du sichuan',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11081:poivre du sichuan', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 345, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 37.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 10.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 25.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poivre du Sichuan', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11081', to_jsonb('Epice (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poivre noir en grains' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poivre noir en grains', 'poivre noir en grains', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poivre noir en grains', 'poivre noir en grains',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11015:poivre noir en grains', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 330, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 39.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 7.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 25.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poivre noir en grains', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11015', to_jsonb('Poivre noir, poudre'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poivre noir moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poivre noir moulu', 'poivre noir moulu', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poivre noir moulu', 'poivre noir moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11015:poivre noir moulu', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 330, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 39.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 7.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 25.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poivre noir moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11015', to_jsonb('Poivre noir, poudre'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poivron jaune frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poivron jaune frais', 'poivron jaune frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poivron jaune frais', 'poivron jaune frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20168:poivron jaune frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 30.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.94, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.73, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poivron jaune frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20168', to_jsonb('Poivron jaune, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poivron rouge frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poivron rouge frais', 'poivron rouge frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poivron rouge frais', 'poivron rouge frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20087:poivron rouge frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 36.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.06, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5.98, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poivron rouge frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20087', to_jsonb('Poivron rouge, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poivron vert frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poivron vert frais', 'poivron vert frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poivron vert frais', 'poivron vert frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20085:poivron vert frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 26, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.81, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.43, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poivron vert frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20085', to_jsonb('Poivron vert, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pomme de terre crue avec peau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pomme de terre crue', 'pomme de terre crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pomme de terre crue, avec peau', 'pomme de terre crue avec peau',
       NULL, 'raw', NULL,
       NULL, 'with_skin',
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '4007:pomme de terre crue avec peau', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 77, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.44, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 17.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pomme de terre crue, avec peau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '4007', to_jsonb('Pomme de terre nouvelle, bouillie/cuite à l''eau'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pomme de terre crue epluchee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pomme de terre crue', 'pomme de terre crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pomme de terre crue, épluchée', 'pomme de terre crue epluchee',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '4008:pomme de terre crue epluchee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 80.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.16, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 16.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.18, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pomme de terre crue, épluchée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '4008', to_jsonb('Pomme de terre, sans peau, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pomme de terre cuite' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pomme de terre cuite', 'pomme de terre cuite', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pomme de terre cuite', 'pomme de terre cuite',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '4048:pomme de terre cuite', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 93.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.01, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 17.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.37, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.96, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pomme de terre cuite', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '4048', to_jsonb('Pomme de terre, cuite (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pomme de terre nouvelle crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pomme de terre nouvelle crue', 'pomme de terre nouvelle crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pomme de terre nouvelle crue', 'pomme de terre nouvelle crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '4023:pomme de terre nouvelle crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 76.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.88, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 15.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pomme de terre nouvelle crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '4023', to_jsonb('Pomme de terre nouvelle, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pomme fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pomme fraîche', 'pomme fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pomme fraîche', 'pomme fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13039:pomme fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 49.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.25, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 11.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.25, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pomme fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13039', to_jsonb('Pomme, pulpe et peau, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'porc hache cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Porc haché cru', 'porc hache cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Porc haché cru', 'porc hache cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '28472:porc hache cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 187, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 18.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.23, 'g', 'estimated'),
      (v_profile, 'fat_g', 12.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Porc haché cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '28472', to_jsonb('Porc, hachage sans jarret, sans bateau, découenné, dégraissé, désossé, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'potimarron frais epepine' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Potimarron frais', 'potimarron frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Potimarron frais, épépiné', 'potimarron frais epepine',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20132:potimarron frais epepine', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 15.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.63, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Potimarron frais, épépiné', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20132', to_jsonb('Potimarron, pulpe, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poule crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poule crue', 'poule crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poule crue', 'poule crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36001:poule crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 124, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 4.51, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poule crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36001', to_jsonb('Poule, viande ,crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'volailles';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poulet cuit effiloche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poulet cuit effiloché', 'poulet cuit effiloche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poulet cuit effiloché', 'poulet cuit effiloche',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36030:poulet cuit effiloche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 188, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 24.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 9.52, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poulet cuit effiloché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36030', to_jsonb('Poulet, cuisse, viande, bouilli/cuit à l''eau'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'volailles';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poulet entier cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poulet entier cru', 'poulet entier cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poulet entier cru', 'poulet entier cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36016:poulet entier cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 173, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 10.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poulet entier cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36016', to_jsonb('Poulet, viande et peau, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'volailles';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poulet entier cru en morceaux' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poulet entier cru en morceaux', 'poulet entier cru en morceaux', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poulet entier cru en morceaux', 'poulet entier cru en morceaux',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36016:poulet entier cru en morceaux', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 173, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 10.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poulet entier cru en morceaux', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36016', to_jsonb('Poulet, viande et peau, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'volailles';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poulet entier cru pret a cuire' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poulet entier cru', 'poulet entier cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poulet entier cru, prêt à cuire', 'poulet entier cru pret a cuire',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '36003:poulet entier cru pret a cuire', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 113, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.73, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poulet entier cru, prêt à cuire', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '36003', to_jsonb('Poulet, viande, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'poulpe cuit' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Poulpe cuit', 'poulpe cuit', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Poulpe cuit', 'poulpe cuit',
       NULL, 'cooked', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '10079:poulpe cuit', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 156, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 29.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.08, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Poulpe cuit', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '10079', to_jsonb('Poulpe, cuit'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pousse de bambou' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pousse de bambou', 'pousse de bambou', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pousse de bambou', 'pousse de bambou',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20198:pousse de bambou', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 35.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.52, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5.08, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.45, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pousse de bambou', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20198', to_jsonb('Bambou, pousse, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pousse de soja fraiche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pousse de soja fraîche', 'pousse de soja fraiche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pousse de soja fraîche', 'pousse de soja fraiche',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20183:pousse de soja fraiche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 28.4, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.81, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pousse de soja fraîche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20183', to_jsonb('Haricot mungo germé ou pousse de "soja", cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pruneau denoyaute' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pruneau dénoyauté', 'pruneau denoyaute', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pruneau dénoyauté', 'pruneau denoyaute',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13042:pruneau denoyaute', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 229, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.63, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 55.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pruneau dénoyauté', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13042', to_jsonb('Pruneau, sec'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'pul biber' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Pul biber', 'pul biber', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Pul biber', 'pul biber',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11088:pul biber', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 376, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 29.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 17.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 27.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Pul biber', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11088', to_jsonb('Poivre de Cayenne ou piment de Cayenne'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'quesillo oaxaca' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Quesillo Oaxaca', 'quesillo oaxaca', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Quesillo Oaxaca', 'quesillo oaxaca',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '170845:quesillo oaxaca', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 299, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 22.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 22.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Quesillo Oaxaca', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '170845', to_jsonb('Cheese, mozzarella, whole milk'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'quinoa cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Quinoa cru', 'quinoa cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Quinoa cru', 'quinoa cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9340:quinoa cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 354, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.1, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 58.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 6.07, 'g', 'estimated'),
      (v_profile, 'fiber_g', 7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Quinoa cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9340', to_jsonb('Quinoa, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'radis daikon cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Radis daikon cru', 'radis daikon cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Radis daikon cru', 'radis daikon cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '168451:radis daikon cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 18, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Radis daikon cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '168451', to_jsonb('Radishes, oriental, raw'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'radis frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Radis frais', 'radis frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Radis frais', 'radis frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20045:radis frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 14.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.94, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.53, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Radis frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20045', to_jsonb('Radis rouge, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'raisin sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Raisin sec', 'raisin sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Raisin sec', 'raisin sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13046:raisin sec', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 321, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 73.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Raisin sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13046', to_jsonb('Raisin, sec'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ras el hanout' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ras el-hanout', 'ras el hanout', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ras el-hanout', 'ras el hanout',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11005:ras el hanout', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 301, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 53.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ras el-hanout', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11005', to_jsonb('Curry, poudre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'reblochon au lait cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Reblochon au lait cru', 'reblochon au lait cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Reblochon au lait cru', 'reblochon au lait cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '12034:reblochon au lait cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 322, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 26.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Reblochon au lait cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '12034', to_jsonb('Fromage à pâte molle et croûte lavée (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'rhum ambre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Rhum ambré', 'rhum ambre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Rhum ambré', 'rhum ambre',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '1004:rhum ambre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 256, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.95, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Rhum ambré', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '1004', to_jsonb('Rhum'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz arborio cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz arborio cru', 'riz arborio cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz arborio cru', 'riz arborio cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9100:riz arborio cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.91, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz arborio cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9100', to_jsonb('Riz blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz basmati cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz basmati cru', 'riz basmati cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz basmati cru', 'riz basmati cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9119:riz basmati cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 351, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 8.06, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 77.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.84, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.92, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz basmati cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9119', to_jsonb('Riz thaï ou basmati, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz bomba cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz bomba cru', 'riz bomba cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz bomba cru', 'riz bomba cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '168881:riz bomba cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 358, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 6.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 79.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.52, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz bomba cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '168881', to_jsonb('Rice, white, short-grain, enriched, uncooked'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz brise cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz brisé cru', 'riz brise cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz brisé cru', 'riz brise cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9100:riz brise cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.91, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz brisé cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9100', to_jsonb('Riz blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz cru', 'riz cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz cru', 'riz cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9100:riz cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.91, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9100', to_jsonb('Riz blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz etuve cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz étuvé cru', 'riz etuve cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz étuvé cru', 'riz etuve cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9101:riz etuve cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 354, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.47, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.98, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.32, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz étuvé cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9101', to_jsonb('Riz blanc étuvé, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz japonais cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz japonais cru', 'riz japonais cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz japonais cru', 'riz japonais cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9100:riz japonais cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.91, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz japonais cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9100', to_jsonb('Riz blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz jasmin cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz jasmin cru', 'riz jasmin cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz jasmin cru', 'riz jasmin cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9100:riz jasmin cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.91, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz jasmin cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9100', to_jsonb('Riz blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz long blanc cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz long blanc cru', 'riz long blanc cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz long blanc cru', 'riz long blanc cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9100:riz long blanc cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.91, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz long blanc cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9100', to_jsonb('Riz blanc, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz rond blanc cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz rond blanc cru', 'riz rond blanc cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz rond blanc cru', 'riz rond blanc cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9100:riz rond blanc cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.91, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz rond blanc cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9100', to_jsonb('Riz blanc, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz rond coreen cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz rond coréen cru', 'riz rond coreen cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz rond coréen cru', 'riz rond coreen cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9100:riz rond coreen cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.91, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz rond coréen cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9100', to_jsonb('Riz blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'riz rond cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Riz rond cru', 'riz rond cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Riz rond cru', 'riz rond cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9100:riz rond cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 350, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 78, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.91, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.05, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Riz rond cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9100', to_jsonb('Riz blanc, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'romarin frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Romarin frais', 'romarin frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Romarin frais', 'romarin frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11068:romarin frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 121, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.31, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 6.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.86, 'g', 'estimated'),
      (v_profile, 'fiber_g', 14.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Romarin frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11068', to_jsonb('Romarin, frais'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'rouget grondin cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Rouget grondin cru', 'rouget grondin cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Rouget grondin cru', 'rouget grondin cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26106:rouget grondin cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 94, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Rouget grondin cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26106', to_jsonb('Grondin, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'safran' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Safran', 'safran', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Safran', 'safran',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11039:safran', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 352, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 11.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 61.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.85, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Safran', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11039', to_jsonb('Safran'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'matieres_grasses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'saindoux' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Saindoux', 'saindoux', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Saindoux', 'saindoux',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '16520:saindoux', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 896, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 99.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Saindoux', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '16520', to_jsonb('Saindoux'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'saint pierre cru' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Saint-pierre cru', 'saint pierre cru', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Saint-pierre cru', 'saint pierre cru',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26104:saint pierre cru', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 86.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.9, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Saint-pierre cru', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26104', to_jsonb('Saint-Pierre, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sake' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Saké', 'sake', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Saké', 'sake',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '1026:sake', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 133, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.99, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Saké', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '1026', to_jsonb('Saké ou Alcool de riz'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sake de cuisine' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Saké de cuisine', 'sake de cuisine', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Saké de cuisine', 'sake de cuisine',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '1026:sake de cuisine', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 133, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.99, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Saké de cuisine', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '1026', to_jsonb('Saké ou Alcool de riz'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'salsa roja' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Salsa roja', 'salsa roja', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Salsa roja', 'salsa roja',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20169:salsa roja', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 26.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Salsa roja', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20169', to_jsonb('Tomate, pulpe, appertisée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'salsa verde' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Salsa verde', 'salsa verde', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Salsa verde', 'salsa verde',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '171824:salsa verde', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 38, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.13, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 6.36, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.89, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Salsa verde', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '171824', to_jsonb('Sauce, salsa, verde, ready-to-serve'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sambal' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sambal', 'sambal', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sambal', 'sambal',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11112:sambal', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 66.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.72, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 7.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sambal', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11112', to_jsonb('Harissa (sauce condimentaire), préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sambar powder' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sambar powder', 'sambar powder', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sambar powder', 'sambar powder',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11005:sambar powder', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 301, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 14, 'g', 'estimated'),
      (v_profile, 'fiber_g', 53.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sambar powder', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11005', to_jsonb('Curry, poudre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce hoisin' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce hoisin', 'sauce hoisin', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce hoisin', 'sauce hoisin',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11100:sauce hoisin', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 136, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.56, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 30.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce hoisin', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11100', to_jsonb('Sauce barbecue, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce huitre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce huître', 'sauce huitre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce huître', 'sauce huitre',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '174529:sauce huitre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 51, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.35, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 10.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.25, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.2, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce huître', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '174529', to_jsonb('Sauce, oyster, ready-to-serve'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce poisson' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce poisson', 'sauce poisson', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce poisson', 'sauce poisson',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11194:sauce poisson', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 81.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 10.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.2, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce poisson', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11194', to_jsonb('Sauce Nuoc Mâm ou Sauce au poisson, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce soja' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce soja', 'sauce soja', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce soja', 'sauce soja',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11104:sauce soja', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.08, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.64, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.86, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.18, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce soja', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11104', to_jsonb('Sauce soja, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce soja claire' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce soja claire', 'sauce soja claire', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce soja claire', 'sauce soja claire',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11104:sauce soja claire', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.08, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.64, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.86, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.18, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce soja claire', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11104', to_jsonb('Sauce soja, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce soja coreenne' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce soja coréenne', 'sauce soja coreenne', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce soja coréenne', 'sauce soja coreenne',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11104:sauce soja coreenne', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.08, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.64, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.86, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.18, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce soja coréenne', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11104', to_jsonb('Sauce soja, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce soja foncee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce soja foncée', 'sauce soja foncee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce soja foncée', 'sauce soja foncee',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11104:sauce soja foncee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.08, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.64, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.86, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.18, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce soja foncée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11104', to_jsonb('Sauce soja, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce soja japonaise' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce soja japonaise', 'sauce soja japonaise', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce soja japonaise', 'sauce soja japonaise',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11104:sauce soja japonaise', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.08, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.64, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.86, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.18, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce soja japonaise', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11104', to_jsonb('Sauce soja, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce soja legere' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce soja légère', 'sauce soja legere', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce soja légère', 'sauce soja legere',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11104:sauce soja legere', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.08, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.64, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.86, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.18, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce soja légère', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11104', to_jsonb('Sauce soja, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce soja legere speciale poisson' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce soja légère spéciale poisson', 'sauce soja legere speciale poisson', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce soja légère spéciale poisson', 'sauce soja legere speciale poisson',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11104:sauce soja legere speciale poisson', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.08, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.64, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.86, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.18, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce soja légère spéciale poisson', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11104', to_jsonb('Sauce soja, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce soja philippine' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce soja philippine', 'sauce soja philippine', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce soja philippine', 'sauce soja philippine',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11104:sauce soja philippine', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.08, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.64, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.86, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.18, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce soja philippine', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11104', to_jsonb('Sauce soja, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce worcestershire' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce Worcestershire', 'sauce worcestershire', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce Worcestershire', 'sauce worcestershire',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '171610:sauce worcestershire', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 77, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 19.2, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.09, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce Worcestershire', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '171610', to_jsonb('Sauce, worcestershire'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sauce yakisoba' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sauce yakisoba', 'sauce yakisoba', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sauce yakisoba', 'sauce yakisoba',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11100:sauce yakisoba', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 136, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.56, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 30.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.15, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sauce yakisoba', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11100', to_jsonb('Sauce barbecue, préemballée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'saucisse chinoise lap cheong' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Saucisse chinoise lap cheong', 'saucisse chinoise lap cheong', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Saucisse chinoise lap cheong', 'saucisse chinoise lap cheong',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '30309:saucisse chinoise lap cheong', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 451, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 27.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 37.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.59, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Saucisse chinoise lap cheong', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '30309', to_jsonb('Saucisse sèche'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'saucisse de montbeliard' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Saucisse de Montbéliard', 'saucisse de montbeliard', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Saucisse de Montbéliard', 'saucisse de montbeliard',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '30105:saucisse de montbeliard', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 319, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 17.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1, 'g', 'estimated'),
      (v_profile, 'fat_g', 27.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Saucisse de Montbéliard', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '30105', to_jsonb('Saucisse de Montbéliard'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'saucisse de morteau' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Saucisse de Morteau', 'saucisse de morteau', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Saucisse de Morteau', 'saucisse de morteau',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '30104:saucisse de morteau', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 397, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 16, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1, 'g', 'estimated'),
      (v_profile, 'fat_g', 36.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Saucisse de Morteau', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '30104', to_jsonb('Saucisse de Morteau'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_transformes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'saucisse de toulouse crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Saucisse de Toulouse crue', 'saucisse de toulouse crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Saucisse de Toulouse crue', 'saucisse de toulouse crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '30110:saucisse de toulouse crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 327, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 13.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1, 'g', 'estimated'),
      (v_profile, 'fat_g', 30, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.64, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Saucisse de Toulouse crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '30110', to_jsonb('Saucisse de Toulouse, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'saumure legere' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Saumure légère', 'saumure legere', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Saumure légère', 'saumure legere',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '18066:saumure legere', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 0, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.03, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Saumure légère', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '18066', to_jsonb('Eau du robinet'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sel fin' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sel fin', 'sel fin', v_category, 'candidate', 'A')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sel fin', 'sel fin',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'A',
       'A', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, 'override:sel fin', 100,
       'g', 'v3-2026-07', 'A', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 0, 'kcal', 'measured'),
      (v_profile, 'protein_g', 0, 'g', 'measured'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'measured'),
      (v_profile, 'fat_g', 0, 'g', 'measured');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sel fin', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     'override:sel fin', to_jsonb('Sel fin'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'semoule de ble dur moyenne seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Semoule de blé dur moyenne sèche', 'semoule de ble dur moyenne seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Semoule de blé dur moyenne sèche', 'semoule de ble dur moyenne seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9610:semoule de ble dur moyenne seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 348, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 71.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.25, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3.37, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Semoule de blé dur moyenne sèche', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9610', to_jsonb('Semoule de blé dur, crue'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sesame' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sésame', 'sesame', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sésame', 'sesame',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15010:sesame', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 557.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.8, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 9.85, 'g', 'estimated'),
      (v_profile, 'fat_g', 49.7, 'g', 'estimated'),
      (v_profile, 'fiber_g', 14.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sésame', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15010', to_jsonb('Sésame, graine'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sesame grille' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sésame grillé', 'sesame grille', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sésame grillé', 'sesame grille',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15038:sesame grille', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 536.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 9.14, 'g', 'estimated'),
      (v_profile, 'fat_g', 48, 'g', 'estimated'),
      (v_profile, 'fiber_g', 16.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sésame grillé', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15038', to_jsonb('Sésame, grillé, graine décortiquée'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'shiitake frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Shiitaké frais', 'shiitake frais', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Shiitaké frais', 'shiitake frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20010:shiitake frais', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 21.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.37, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.88, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.23, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.72, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Shiitaké frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20010', to_jsonb('Champignon, tout type, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'shiitake seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Shiitaké séché', 'shiitake seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Shiitaké séché', 'shiitake seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20211:shiitake seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 316, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.58, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 63.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.99, 'g', 'estimated'),
      (v_profile, 'fiber_g', 11.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Shiitaké séché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20211', to_jsonb('Champignon, lentin comestible ou shiitaké, séché'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'spaghetti secs' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Spaghetti secs', 'spaghetti secs', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Spaghetti secs', 'spaghetti secs',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9810:spaghetti secs', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 331, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 65.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.79, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Spaghetti secs', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9810', to_jsonb('Pâtes sèches standard, crues'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'ssamjang' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Ssamjang', 'ssamjang', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Ssamjang', 'ssamjang',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20916:ssamjang', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 163, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 11.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 18.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.31, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.4, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Ssamjang', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20916', to_jsonb('Miso'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sucre brun' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sucre brun', 'sucre brun', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sucre brun', 'sucre brun',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31017:sucre brun', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 389.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 97.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sucre brun', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31017', to_jsonb('Sucre roux'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sucre candi' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sucre candi', 'sucre candi', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sucre candi', 'sucre candi',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31016:sucre candi', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 399.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 99.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sucre candi', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31016', to_jsonb('Sucre blanc'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sucre de palme' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sucre de palme', 'sucre de palme', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sucre de palme', 'sucre de palme',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31017:sucre de palme', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 389.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.12, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 97.3, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sucre de palme', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31017', to_jsonb('Sucre roux'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_sucres';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sucre semoule' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sucre semoule', 'sucre semoule', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sucre semoule', 'sucre semoule',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '31016:sucre semoule', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 399.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 99.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sucre semoule', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '31016', to_jsonb('Sucre blanc'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'sumac moulu' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Sumac moulu', 'sumac moulu', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Sumac moulu', 'sumac moulu',
       'powder', NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11081:sumac moulu', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 345, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 37.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 10.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 25.9, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Sumac moulu', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11081', to_jsonb('Epice (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumineuses';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tahini' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tahini', 'tahini', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tahini', 'tahini',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '15203:tahini', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 606.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.3, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 13.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 53.4, 'g', 'estimated'),
      (v_profile, 'fiber_g', 7, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tahini', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '15203', to_jsonb('Tahin ou Purée de sésame'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tamarin en pate' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tamarin en pâte', 'tamarin en pate', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tamarin en pâte', 'tamarin en pate',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13080:tamarin en pate', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 245.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 2.65, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 57.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.6, 'g', 'estimated'),
      (v_profile, 'fiber_g', 5.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tamarin en pâte', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13080', to_jsonb('Tamarin, fruit mûr, pulpe, cru'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tare de soja' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tare de soja', 'tare de soja', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tare de soja', 'tare de soja',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '171167:tare de soja', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 89, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 5.93, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 15.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.02, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.15, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tare de soja', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '171167', to_jsonb('Sauce, teriyaki, ready-to-serve'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'thon au naturel en conserve egoutte' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Thon au naturel en conserve', 'thon au naturel en conserve', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Thon au naturel en conserve, égoutté', 'thon au naturel en conserve egoutte',
       NULL, NULL, 'canned',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26181:thon au naturel en conserve egoutte', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 128, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 26.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.58, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.98, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Thon au naturel en conserve, égoutté', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26181', to_jsonb('Thon albacore ou thon jaune, au naturel, appertisé, égoutté'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'thym frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Thym frais', 'thym frais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Thym frais', 'thym frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11070:thym frais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 107, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 5.56, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 10.5, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.68, 'g', 'estimated'),
      (v_profile, 'fiber_g', 14, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Thym frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11070', to_jsonb('Thym, frais'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'herbes_aromates';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'thym seche' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Thym séché', 'thym seche', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Thym séché', 'thym seche',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11038:thym seche', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 285, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.11, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 26.9, 'g', 'estimated'),
      (v_profile, 'fat_g', 7.43, 'g', 'estimated'),
      (v_profile, 'fiber_g', 37, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Thym séché', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11038', to_jsonb('Thym, séché'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'viandes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tofu ferme' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tofu ferme', 'tofu ferme', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tofu ferme', 'tofu ferme',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20904:tofu ferme', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 143, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 14.7, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.87, 'g', 'estimated'),
      (v_profile, 'fat_g', 8.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.5, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tofu ferme', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20904', to_jsonb('Tofu nature, préemballé'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tofu soyeux ferme' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tofu soyeux ferme', 'tofu soyeux ferme', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tofu soyeux ferme', 'tofu soyeux ferme',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20906:tofu soyeux ferme', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 52.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.53, 'g', 'estimated'),
      (v_profile, 'fat_g', 2.9, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tofu soyeux ferme', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20906', to_jsonb('Tofu soyeux, préemballé'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tomate concassee en conserve' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tomate concassée en conserve', 'tomate concassee en conserve', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tomate concassée en conserve', 'tomate concassee en conserve',
       NULL, NULL, 'canned',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20169:tomate concassee en conserve', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 26.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tomate concassée en conserve', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20169', to_jsonb('Tomate, pulpe, appertisée'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tomate concentree' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tomate concentrée', 'tomate concentree', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tomate concentrée', 'tomate concentree',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20068:tomate concentree', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 99.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 4.4, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 17.1, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.53, 'g', 'estimated'),
      (v_profile, 'fiber_g', 4.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tomate concentrée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20068', to_jsonb('Tomate, concentré, appertisé'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tomate entiere pelee en conserve' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tomate entière pelée en conserve', 'tomate entiere pelee en conserve', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tomate entière pelée en conserve', 'tomate entiere pelee en conserve',
       NULL, NULL, 'canned',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20048:tomate entiere pelee en conserve', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 18.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.07, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 1.89, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.28, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.27, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tomate entière pelée en conserve', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20048', to_jsonb('Tomate, pelée, appertisée, égouttée'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tomate fraiche mure' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tomate fraîche mûre', 'tomate fraiche mure', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tomate fraîche mûre', 'tomate fraiche mure',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20047:tomate fraiche mure', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 19.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.86, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.49, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.26, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'u', 'g', 120, 'poids unitaire culinaire revu',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tomate fraîche mûre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20047', to_jsonb('Tomate, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tomate rapee' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tomate râpée', 'tomate rapee', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tomate râpée', 'tomate rapee',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20047:tomate rapee', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 19.3, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.86, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.49, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.26, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.2, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tomate râpée', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20047', to_jsonb('Tomate, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'legumes';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tomatillo frais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tomatillo frais', 'tomatillo frais', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tomatillo frais', 'tomatillo frais',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '20119:tomatillo frais', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.2, 'g', 'estimated'),
      (v_profile, 'fiber_g', 1.1, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tomatillo frais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '20119', to_jsonb('Tomate verte, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tome fraiche de l aubrac' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tome fraîche de l’Aubrac', 'tome fraiche de l aubrac', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tome fraîche de l’Aubrac', 'tome fraiche de l aubrac',
       NULL, NULL, 'fresh',
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '12034:tome fraiche de l aubrac', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 322, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 20.2, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 26.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tome fraîche de l’Aubrac', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '12034', to_jsonb('Fromage à pâte molle et croûte lavée (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tonnarelli secs' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tonnarelli secs', 'tonnarelli secs', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tonnarelli secs', 'tonnarelli secs',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9810:tonnarelli secs', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 331, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 65.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.79, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tonnarelli secs', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9810', to_jsonb('Pâtes sèches standard, crues'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'tortilla de mais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Tortilla de maïs', 'tortilla de mais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Tortilla de maïs', 'tortilla de mais',
       NULL, NULL, NULL,
       NULL, NULL,
       'u', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '7813:tortilla de mais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 313, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 9.06, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 54.7, 'g', 'estimated'),
      (v_profile, 'fat_g', 5.8, 'g', 'estimated'),
      (v_profile, 'fiber_g', 2.8, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Tortilla de maïs', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '7813', to_jsonb('Tortilla souple (à garnir), à base de maïs'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'poissons_fruits_de_mer';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'truite crue' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Truite crue', 'truite crue', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Truite crue', 'truite crue',
       NULL, 'raw', NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '26092:truite crue', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 111, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 19.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.63, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Truite crue', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '26092', to_jsonb('Truite de mer, crue'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'epices';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vanille liquide' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vanille liquide', 'vanille liquide', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vanille liquide', 'vanille liquide',
       'liquid', NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11098:vanille liquide', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 57.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.03, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 14.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vanille liquide', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11098', to_jsonb('Vanille, extrait aqueux'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'cereales_feculents';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vermicelle fin sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vermicelle fin sec', 'vermicelle fin sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vermicelle fin sec', 'vermicelle fin sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '9810:vermicelle fin sec', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 331, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 12.6, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 65.8, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.79, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vermicelle fin sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '9810', to_jsonb('Pâtes sèches standard, crues'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vin blanc sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vin blanc sec', 'vin blanc sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vin blanc sec', 'vin blanc sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '5215:vin blanc sec', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 18.2, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.04, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vin blanc sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '5215', to_jsonb('Vin blanc (sec)'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vin blanc sec d alsace' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vin blanc sec d Alsace', 'vin blanc sec d alsace', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vin blanc sec d Alsace', 'vin blanc sec d alsace',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '174837:vin blanc sec d alsace', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 82, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.07, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.6, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.99, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vin blanc sec d Alsace', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '174837', to_jsonb('Alcoholic beverage, wine, table, white'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vin de riz taiwanais' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vin de riz taïwanais', 'vin de riz taiwanais', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vin de riz taïwanais', 'vin de riz taiwanais',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '1026:vin de riz taiwanais', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 133, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vin de riz taïwanais', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '1026', to_jsonb('Saké ou Alcool de riz'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vin rouge de bourgogne' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vin rouge de Bourgogne', 'vin rouge de bourgogne', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vin rouge de Bourgogne', 'vin rouge de bourgogne',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '5214:vin rouge de bourgogne', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 82.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.07, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 0.99, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vin rouge de Bourgogne', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '5214', to_jsonb('Vin rouge'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vin rouge sec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vin rouge sec', 'vin rouge sec', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vin rouge sec', 'vin rouge sec',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '5214:vin rouge sec', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 82.1, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.07, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.63, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vin rouge sec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '5214', to_jsonb('Vin rouge'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vin shaoxing' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vin Shaoxing', 'vin shaoxing', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vin Shaoxing', 'vin shaoxing',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '1026:vin shaoxing', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 133, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vin Shaoxing', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '1026', to_jsonb('Saké ou Alcool de riz'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vinaigre blanc' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vinaigre blanc', 'vinaigre blanc', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vinaigre blanc', 'vinaigre blanc',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11018:vinaigre blanc', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.04, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vinaigre blanc', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11018', to_jsonb('Vinaigre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vinaigre de canne' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vinaigre de canne', 'vinaigre de canne', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vinaigre de canne', 'vinaigre de canne',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11018:vinaigre de canne', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.04, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.01, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vinaigre de canne', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11018', to_jsonb('Vinaigre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vinaigre de cidre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vinaigre de cidre', 'vinaigre de cidre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vinaigre de cidre', 'vinaigre de cidre',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11018:vinaigre de cidre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.04, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vinaigre de cidre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11018', to_jsonb('Vinaigre'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vinaigre de malt' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vinaigre de malt', 'vinaigre de malt', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vinaigre de malt', 'vinaigre de malt',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11018:vinaigre de malt', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.04, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.01, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vinaigre de malt', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11018', to_jsonb('Vinaigre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vinaigre de palmier ou cidre' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vinaigre de palmier ou cidre', 'vinaigre de palmier ou cidre', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vinaigre de palmier ou cidre', 'vinaigre de palmier ou cidre',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11018:vinaigre de palmier ou cidre', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.04, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.01, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vinaigre de palmier ou cidre', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11018', to_jsonb('Vinaigre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vinaigre de riz' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vinaigre de riz', 'vinaigre de riz', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vinaigre de riz', 'vinaigre de riz',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11018:vinaigre de riz', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.04, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vinaigre de riz', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11018', to_jsonb('Vinaigre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vinaigre de vin rouge' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vinaigre de vin rouge', 'vinaigre de vin rouge', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vinaigre de vin rouge', 'vinaigre de vin rouge',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11220:vinaigre de vin rouge', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 21.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.5, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.27, 'g', 'estimated'),
      (v_profile, 'fat_g', 0, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vinaigre de vin rouge', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11220', to_jsonb('Vinaigre de vin rouge'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vinaigre de xeres' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vinaigre de xérès', 'vinaigre de xeres', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vinaigre de xérès', 'vinaigre de xeres',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11018:vinaigre de xeres', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.04, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vinaigre de xérès', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11018', to_jsonb('Vinaigre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'condiments_sauces';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'vinaigre noir chinois' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Vinaigre noir chinois', 'vinaigre noir chinois', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Vinaigre noir chinois', 'vinaigre noir chinois',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '11018:vinaigre noir chinois', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 22.6, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.04, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 0.62, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.1, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Vinaigre noir chinois', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '11018', to_jsonb('Vinaigre'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'yaourt grec' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Yaourt grec', 'yaourt grec', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Yaourt grec', 'yaourt grec',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19646:yaourt grec', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 77.5, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 7.86, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 3.46, 'g', 'estimated'),
      (v_profile, 'fat_g', 3.26, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.83, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Yaourt grec', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19646', to_jsonb('Fromage blanc nature, 3% MG environ'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'produits_laitiers';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'yaourt nature' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Yaourt nature', 'yaourt nature', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Yaourt nature', 'yaourt nature',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '19593:yaourt nature', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 45.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 3.88, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 2.65, 'g', 'estimated'),
      (v_profile, 'fat_g', 1.5, 'g', 'estimated'),
      (v_profile, 'fiber_g', 3, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Yaourt nature', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '19593', to_jsonb('Yaourt, lait fermenté ou spécialité laitière, nature'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'boissons';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'zakwas de betterave fermente' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Zakwas de betterave fermenté', 'zakwas de betterave fermente', v_category, 'candidate', 'C')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Zakwas de betterave fermenté', 'zakwas de betterave fermente',
       NULL, NULL, NULL,
       NULL, NULL,
       'ml', 1, 'candidate', 'C',
       'C', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '2075:zakwas de betterave fermente', 100,
       'g', 'v3-2026-07', 'C', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 21.7, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 0.72, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 4.66, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.13, 'g', 'estimated'),
      (v_profile, 'fiber_g', 0.37, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'ml', 'g', 1.02, 'densité alimentaire revue',
     v_dataset, 'B', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Zakwas de betterave fermenté', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '2075', to_jsonb('Jus de légumes, pur jus (aliment moyen)'::text),
     'explicit_proxy_with_note', v_run, true);
END
$food$;

DO $food$
DECLARE
  v_dataset uuid;
  v_run uuid;
  v_category uuid;
  v_concept uuid;
  v_form uuid;
  v_profile uuid;
BEGIN
  SELECT id INTO v_dataset FROM ops.source_datasets WHERE code = 'myko_recipe_food_v3';
  SELECT id INTO v_run FROM ops.import_runs WHERE configuration_hash = '6cad0dc95a0f297f615e18b55b3e41a3' LIMIT 1;
  SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'fruits';
  IF v_category IS NULL THEN
    SELECT id INTO v_category FROM catalog.food_categories WHERE code = 'preparations_culinaires';
  END IF;

  SELECT ff.id INTO v_form
  FROM catalog.food_forms ff
  WHERE ff.canonical_name_normalized = 'zeste de citron' AND ff.status <> 'rejected'
  ORDER BY CASE ff.status WHEN 'published' THEN 0 ELSE 1 END, ff.confidence_level
  LIMIT 1;

  IF v_form IS NULL THEN
    INSERT INTO catalog.food_concepts
      (canonical_name, canonical_name_normalized, category_id, status, confidence_level)
    VALUES ('Zeste de citron', 'zeste de citron', v_category, 'candidate', 'B')
    ON CONFLICT (canonical_name_normalized) DO UPDATE SET updated_at = now()
    RETURNING id INTO v_concept;

    INSERT INTO catalog.food_forms
      (food_concept_id, canonical_name, canonical_name_normalized,
       physical_state, cooking_state, preservation_state, bone_state, skin_state,
       default_quantity_unit, edible_yield_ratio, status, confidence_level,
       identity_confidence, category_confidence, state_confidence)
    VALUES
      (v_concept, 'Zeste de citron', 'zeste de citron',
       NULL, NULL, NULL,
       NULL, NULL,
       'g', 1, 'candidate', 'B',
       'B', 'B', 'B')
    RETURNING id INTO v_form;
  END IF;

  SELECT id INTO v_profile
  FROM catalog.food_nutrition_profiles
  WHERE food_form_id = v_form AND is_primary
  LIMIT 1;

  IF v_profile IS NULL THEN
    INSERT INTO catalog.food_nutrition_profiles
      (food_form_id, source_dataset_id, source_record_key, basis_quantity,
       basis_unit, data_version, confidence_level, is_primary)
    VALUES
      (v_form, v_dataset, '13125:zeste de citron', 100,
       'g', 'v3-2026-07', 'B', true)
    ON CONFLICT (source_dataset_id, source_record_key, data_version) DO UPDATE
      SET food_form_id = EXCLUDED.food_form_id, confidence_level = EXCLUDED.confidence_level
    RETURNING id INTO v_profile;

    DELETE FROM catalog.food_nutrient_values WHERE nutrition_profile_id = v_profile;
    INSERT INTO catalog.food_nutrient_values
      (nutrition_profile_id, nutrient_code, amount, unit, value_status)
    VALUES
      (v_profile, 'energy_kcal', 29.8, 'kcal', 'estimated'),
      (v_profile, 'protein_g', 1.38, 'g', 'estimated'),
      (v_profile, 'carbohydrate_g', 5.4, 'g', 'estimated'),
      (v_profile, 'fat_g', 0.3, 'g', 'estimated'),
      (v_profile, 'fiber_g', 10.6, 'g', 'estimated');
  END IF;

  DELETE FROM catalog.food_unit_conversions
  WHERE food_form_id = v_form AND source_dataset_id = v_dataset;
  INSERT INTO catalog.food_unit_conversions
    (food_form_id, from_unit, to_unit, factor, context, source_dataset_id, confidence_level, status)
  VALUES
    (v_form, 'g', 'g', 1, 'identité masse',
     v_dataset, 'A', 'candidate')
  ON CONFLICT (food_form_id, from_unit, to_unit, COALESCE(context, '')) DO UPDATE
    SET factor = EXCLUDED.factor, confidence_level = EXCLUDED.confidence_level;

  DELETE FROM quality.review_tasks
  WHERE entity_type = 'food_form' AND entity_id = v_form AND task_type = 'missing_storage_profile';
  INSERT INTO quality.review_tasks
    (entity_type, entity_id, task_type, priority, reason_codes, proposed_changes, status)
  SELECT 'food_form', v_form, 'missing_storage_profile', 3,
         ARRAY['storage_truth_required'],
         jsonb_build_object('form', 'Zeste de citron', 'note', 'Aucune durée générique inventée.'),
         'open'
  WHERE NOT EXISTS (SELECT 1 FROM catalog.food_storage_profiles WHERE food_form_id = v_form);

  DELETE FROM ops.field_provenance
  WHERE entity_schema = 'catalog' AND entity_table = 'food_forms'
    AND entity_id = v_form AND field_name = 'recipe_v3_resolution';
  INSERT INTO ops.field_provenance
    (entity_schema, entity_table, entity_id, field_name, source_dataset_id,
     source_record_key, normalized_value, transformation_rule, import_run_id, selected)
  VALUES
    ('catalog', 'food_forms', v_form, 'recipe_v3_resolution', v_dataset,
     '13125', to_jsonb('Citron, zeste, cru'::text),
     'exact_or_reviewed_ciqual_match', v_run, true);
END
$food$;
